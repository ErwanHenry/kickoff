---
phase: 10
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/unit/og.test.ts
  - src/lib/__tests__/unit/emails.test.ts
  - src/lib/__tests__/unit/merge.test.ts
  - src/lib/__tests__/fixtures/match.ts
  - src/lib/__tests__/fixtures/user.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "All test stub files exist with describe blocks for each requirement"
    - "Each test has clear expected behavior documented in comments"
    - "Fixtures provide realistic test data for OG, emails, and merge scenarios"
  artifacts:
    - path: "src/lib/__tests__/unit/og.test.ts"
      provides: "Test stubs for OG image generation (SHARE-01, SHARE-02)"
      min_lines: 40
    - path: "src/lib/__tests__/unit/emails.test.ts"
      provides: "Test stubs for email notifications (NOTIF-01 through NOTIF-05)"
      min_lines: 60
    - path: "src/lib/__tests__/unit/merge.test.ts"
      provides: "Test stubs for guest-to-user merge (AUTH-05)"
      min_lines: 50
    - path: "src/lib/__tests__/fixtures/match.ts"
      provides: "Match fixtures for OG and email tests"
      min_lines: 30
    - path: "src/lib/__tests__/fixtures/user.ts"
      provides: "User fixtures for merge tests"
      min_lines: 20
  key_links:
    - from: "10-01-PLAN.md (OG implementation)"
      to: "src/lib/__tests__/unit/og.test.ts"
      via: "Tests must pass after implementation"
      pattern: "pnpm test og.test.ts"
    - from: "10-02-PLAN.md (Email implementation)"
      to: "src/lib/__tests__/unit/emails.test.ts"
      via: "Tests must pass after implementation"
      pattern: "pnpm test emails.test.ts"
    - from: "10-03-PLAN.md (Guest merge)"
      to: "src/lib/__tests__/unit/merge.test.ts"
      via: "Tests must pass after implementation"
      pattern: "pnpm test merge.test.ts"
---

<objective>
Create test infrastructure stub files for Phase 10 implementation. These tests define the expected behavior for OG image generation, email notifications, and guest-to-user merge, following TDD methodology.

Purpose: Establish test contracts before implementation ensures all features are verifiable and meet requirements
Output: 5 test files with stub tests that document expected behavior
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-polish-production/10-CONTEXT.md
@.planning/phases/10-polish-production/10-RESEARCH.md
@.planning/phases/10-polish-production/10-VALIDATION.md

# Phase 10 Requirements
From ROADMAP.md Phase 10:
- AUTH-05: Guest can create account and merge all match history
- SHARE-01: Match link generates OG preview for WhatsApp
- SHARE-02: OG image displays match info (title, date, location, confirmed count)
- NOTIF-01: Waitlisted player receives email when promoted to confirmed
- NOTIF-02: Player receives reminder email 2h before confirmation deadline
- NOTIF-03: Players receive email after match to rate teammates
- NOTIF-04: Group members receive email when new weekly match created
- NOTIF-05: New user receives welcome email after account creation

# Existing Test Infrastructure
From package.json:
- Vitest is configured with vitest.config.ts
- Test scripts: `pnpm test`, `pnpm test:ui`, `pnpm test:coverage`
- Testing libraries: @testing-library/react, @testing-library/jest-dom, jsdom

# Design Decisions from CONTEXT.md
D-01 through D-08: OG image design specs (1200x630px, kickoff colors, DM Sans font, truncation rules)
D-09: Plain text emails (simple, fast, works everywhere)
D-11: Email types (waitlist promotion, deadline reminder, post-match rating, welcome)
D-15 through D-18: Guest merge strategy (match_players, ratings, stats recalculation)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OG image test stubs</name>
  <files>src/lib/__tests__/unit/og.test.ts</files>
  <behavior>
    Test suite for OG image generation per CONTEXT.md D-01 through D-08:

    1. "should generate OG image with match title" — ImageResponse called with title from match
    2. "should use fallback title when match has no title" — Falls back to "Match du [date]" format
    3. "should display player count badge with lime background" — #4ADE80 background, dark text
    4. "should truncate location after 25 chars" — Long location shows "UrbanSoccer Nice Étoile..." (ellipsis)
    5. "should use kickoff colors in image" — Background #2D5016, text white, accent #4ADE80
    6. "should format date in French locale" — date-fns with fr locale produces "8 avril 2026 20h00"
    7. "should return 1200x630 image dimensions" — ImageResponse called with width: 1200, height: 630

    Edge cases:
    8. "should handle missing match gracefully" — Returns 404 or default image
    9. "should handle special characters in title" — Emoji and accents render correctly
  </behavior>
  <action>Create src/lib/__tests__/unit/og.test.ts with describe block for OG image generation. Add test.it stubs for each behavior above. Mock @vercel/og ImageResponse. Use match fixtures from fixtures/match.ts. Document expected values in comments (e.g., "expect dimensions to be 1200x630"). DO NOT implement actual tests yet — just describe them.</action>
  <verify>File exists with describe("OG image generation") and 9 test.it() stubs. Each stub has comment documenting expected behavior.</verify>
  <done>OG test stub file created with all 9 test scenarios documented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create email notification test stubs</name>
  <files>src/lib/__tests__/unit/emails.test.ts</files>
  <behavior>
    Test suite for email notifications per CONTEXT.md D-11:

    Waitlist Promotion (NOTIF-01):
    1. "should send waitlist promotion email with player name" — Email includes "Salut [prénom] !"
    2. "should include match link in waitlist promotion" — Email contains match URL

    Deadline Reminder (NOTIF-02):
    3. "should send deadline reminder 2h before deadline" — Checks timing calculation
    4. "should include confirm CTA in reminder" — Email has "→ Confirmer" link

    Post-Match Rating (NOTIF-03):
    5. "should send rating email after match" — Triggers when match status = "played"
    6. "should include rating link in post-match email" — Email contains /m/{token}/rate

    Welcome Email (NOTIF-05):
    7. "should send welcome email after registration" — Called on user creation
    8. "should include player name in welcome email" — Email says "Bienvenue [prénom] !"

    Email Format (D-09: Plain Text):
    9. "should use plain text format for all emails" — No HTML, just text content
    10. "should respect user notification preferences" — Check prefs before sending

    Common:
    11. "should use Resend client from auth.ts" — resend.emails.send() called
    12. "should handle missing email gracefully" — No error if user.email = null
  </behavior>
  <action>Create src/lib/__tests__/unit/emails.test.ts with describe blocks for each email type. Add test.it() stubs for all 12 behaviors. Mock resend.emails.send. Document expected email text in comments (e.g., "expect subject to be 'Bonne nouvelle !'"). Use user fixtures from fixtures/user.ts. DO NOT implement tests — just stub them.</action>
  <verify>File exists with 5 describe blocks (waitlist, reminder, rating, welcome, common) and 12 test.it() stubs.</verify>
  <done>Email test stub file created with all scenarios documented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create guest merge test stubs</name>
  <files>src/lib/__tests__/unit/merge.test.ts</files>
  <behavior>
    Test suite for guest-to-user merge per CONTEXT.md D-15 through D-18:

    Core Merge (AUTH-05):
    1. "should update match_players with new user_id" — All guest_token records updated
    2. "should set guest_name and guest_token to null after merge" — Fields cleared
    3. "should update ratings rater_id from guest_token to user_id" — Ratings reattributed to user
    4. "should update ratings rated_id from guest_token to user_id" — Ratings received updated
    5. "should recalculate player_stats after merge" — Stats reflect merged data

    Edge Cases (D-17):
    6. "should handle guest with no match history" — User created, no errors
    7. "should handle guest with existing account" — Merge onto existing user (not duplicate)
    8. "should handle missing guest_token cookie" — Normal registration, no merge attempt
    9. "should delete guest_token cookie after successful merge" — Cookie removed
    10. "should use transaction for atomic merge" — All updates or none (Drizzle tx)

    Data Integrity:
    11. "should preserve all match history after merge" — All RSVPs visible on profile
    12. "should preserve all ratings given and received" — Rating count unchanged
  </behavior>
  <action>Create src/lib/__tests__/unit/merge.test.ts with describe block for guest merge. Add test.it() stubs for all 12 behaviors. Mock db.transaction, getGuestToken, deleteGuestToken, recalculatePlayerStats. Document expected database state in comments (e.g., "expect match_players.guestToken to be null"). DO NOT implement tests.</action>
  <verify>File exists with describe("guest-to-user merge") and 12 test.it() stubs.</verify>
  <done>Merge test stub file created with all scenarios documented</done>
</task>

<task type="auto">
  <name>Task 4: Create match fixtures</name>
  <files>src/lib/__tests__/fixtures/match.ts</files>
  <action>Create src/lib/__tests__/fixtures/match.ts with export const matchFixtures. Include:
- matchWithTitle: { id, title: "Foot du mardi", location: "UrbanSoccer Nice", date: "2026-04-08T20:00:00Z", maxPlayers: 14, shareToken }
- matchWithoutTitle: { id, title: null, location: "Le Five", date: "2026-04-15T19:00:00Z", maxPlayers: 10, shareToken }
- matchWithLongLocation: { id, title: "Match amical", location: "UrbanSoccer Nice Étoile - Very Long Name That Exceeds", date: "2026-04-08T20:00:00Z", maxPlayers: 12, shareToken }
- matchWithSpecialChars: { id, title: "Match ⚽ duété", location: "Stade de France", date: "2026-07-14T21:00:00Z", maxPlayers: 22, shareToken }

Use nanoid(10) for shareTokens. Export as const array: export const matches = [matchWithTitle, matchWithoutTitle, matchWithLongLocation, matchWithSpecialChars].</action>
  <verify>File exists with 4 match fixtures. All required fields present. shareTokens are 10-char strings.</verify>
  <done>Match fixtures created for OG and email tests</done>
</task>

<task type="auto">
  <name>Task 5: Create user fixtures</name>
  <files>src/lib/__tests__/fixtures/user.ts</files>
  <action>Create src/lib/__tests__/fixtures/user.ts with export const userFixtures. Include:
- userWithEmail: { id, name: "Karim", email: "karim@test.com" }
- userWithoutEmail: { id, name: "Guest Player", email: null }
- newRegisteredUser: { id, name: "Thomas", email: "thomas@test.com" }

Export as const array: export const users = [userWithEmail, userWithoutEmail, newRegisteredUser]. Also export guestTokens for merge tests: export const guestTokens = { valid: "abc123def4", expired: "old123" }.</action>
  <verify>File exists with 3 user fixtures and guestTokens export. All fields present.</verify>
  <done>User fixtures created for merge and email tests</done>
</task>

</tasks>

<verification>
After Wave 0 complete, verify:
1. All 5 test files exist in src/lib/__tests__/ directory
2. `pnpm test --run` executes (tests will fail — this is expected for TDD)
3. Count total test stubs: expect ~33 test.it() blocks across 3 files
4. Each stub has comment documenting expected behavior
5. Fixtures are importable (no syntax errors)
</verification>

<success_criteria>
- Test infrastructure establishes clear contracts for all Phase 10 features
- Each requirement (AUTH-05, SHARE-01, SHARE-02, NOTIF-01 through NOTIF-05) has corresponding test stubs
- Fixtures provide realistic data covering edge cases
- Subsequent plans can implement features to make these tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-production/10-00-SUMMARY.md`
</output>
