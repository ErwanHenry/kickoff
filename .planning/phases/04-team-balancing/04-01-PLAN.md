---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/queries/players.ts
  - src/app/match/[id]/teams/page.tsx
  - src/components/match/balance-indicator.tsx
  - src/components/match/draggable-player-card.tsx
  - src/components/match/team-reveal.tsx
autonomous: true
requirements:
  - BALANCE-01
  - BALANCE-02
  - BALANCE-03
  - BALANCE-04
  - BALANCE-07

must_haves:
  truths:
    - System generates balanced teams using brute-force algorithm (≤14 players) or serpentine (>14 players)
    - Algorithm uses player scores (technique 40%, physique 30%, collectif 30%)
    - New players without ratings default to 3.0 on all axes
    - Organizer can view team assignments with total scores per team
    - Teams display with visual balance indicator (Équilibré / Léger avantage / Déséquilibré)
  artifacts:
    - path: src/lib/db/queries/players.ts
      provides: getMatchTeams query function
      exports: ["getMatchTeams"]
    - path: src/app/match/[id]/teams/page.tsx
      provides: Teams page with generated teams display
      contains: "getMatchTeams"
    - path: src/components/match/balance-indicator.tsx
      provides: Visual balance indicator using design system colors
      contains: "statusBadges|bg-lime-glow|bg-yellow-card|bg-red-card"
    - path: src/components/match/draggable-player-card.tsx
      provides: Player card with FootballIcon and design tokens
      contains: "FootballIcon"
  key_links:
    - from: src/app/match/[id]/teams/page.tsx
      to: src/lib/db/queries/players.ts
      via: getMatchTeams function
      pattern: "getMatchTeams\\(matchId\\)"
    - from: src/components/match/balance-indicator.tsx
      to: src/lib/design-tokens.ts
      via: statusBadges import
      pattern: "from '@/lib/design-tokens'"
    - from: src/components/match/draggable-player-card.tsx
      to: src/components/icons/football-icons.tsx
      via: FootballIcon import
      pattern: "FootballIcon"
---

<objective>
Complete the team balancing feature by adding the missing database query and integrating the design system into the UI components.

Purpose: Enable organizers to generate balanced teams for a match using the existing algorithm, view the generated teams with scores, and see a visual balance indicator. The UI must follow the kickoff design system for consistency.

Output: Working teams page at `/match/[id]/teams` that generates balanced teams, displays them with the design system, and shows the balance indicator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-team-balancing/04-CONTEXT.md

@src/lib/team-balancer.ts
@src/lib/actions/teams.ts
@src/lib/db/queries/players.ts
@src/lib/design-tokens.ts
@src/components/icons/football-icons.tsx
@src/components/match/team-reveal.tsx
@src/components/match/balance-indicator.tsx
@src/components/match/draggable-player-card.tsx
@src/app/match/[id]/teams/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getMatchTeams database query</name>
  <files>src/lib/db/queries/players.ts</files>
  <read_first>
    1. src/lib/db/queries/players.ts (see existing getMatchPlayersWithStats function)
    2. src/db/schema.ts (verify matchPlayers.team column exists)
  </read_first>
  <action>
    Add a new function `getMatchTeams(matchId: string)` to src/lib/db/queries/players.ts that:
    1. Queries match_players filtered by matchId and status='confirmed'
    2. LEFT JOINs with users table to get player names (COALESCE users.name, guestName)
    3. LEFT JOINs with player_stats table to get avg_technique, avg_physique, avg_collectif (prefer group-specific stats if groupId exists)
    4. Filters by team column ('A' or 'B')
    5. Returns a Promise<BalanceResult> with:
       - teamA: Player[] (all players with team='A', including id, name, avg_technique as avgTechnique, avg_physique as avgPhysique, avg_collectif as avgCollectif, total_ratings_received as totalRatings)
       - teamB: Player[] (all players with team='B', same structure)
       - For each team, calculate totalScore (sum of calculatePlayerScore for each player) and playerCount
       - diff: absolute difference between teamA.totalScore and teamB.totalScore
       - algorithm: 'brute-force' (hardcoded since teams were already generated)
    6. Default to 3.0 for missing stats (Number(null) || 3.0)
    7. Convert Decimal types to number using Number()

    Export this function alongside getMatchPlayersWithStats.

    Reference the PlayerWithStats interface already defined in the file, and import BalanceResult and Player types from '@/lib/team-balancer'.
  </action>
  <verify>
    <automated>grep -n "export.*getMatchTeams" src/lib/db/queries/players.ts</automated>
  </verify>
  <done>
    getMatchTeams function exists and exports, returns BalanceResult with teamA/teamB players and scores
  </done>
</task>

<task type="auto">
  <name>Task 2: Update teams page to fetch and display teams</name>
  <files>src/app/match/[id]/teams/page.tsx</files>
  <read_first>
    1. src/app/match/[id]/teams/page.tsx (current implementation with placeholder)
    2. src/lib/db/queries/players.ts (getMatchTeams function from Task 1)
  </read_first>
  <action>
    Update src/app/match/[id]/teams/page.tsx to:
    1. Import getMatchTeams from '@/lib/db/queries/players'
    2. In the "if (match.status !== 'locked')" branch, keep the generate button form (it's correct)
    3. Replace the placeholderTeams with a call to getMatchTeams(id) when match.status === 'locked'
    4. Pass the fetched teams to TeamReveal component (remove isLocked prop or set to false to allow drag-and-drop)
    5. Add a "Remélanger" button that calls generateTeams again to regenerate with a different random combination
    6. Update the page header to show match title and date (from match object)
    7. Use the design system colors: bg-chalk background, text-pitch for headers

    The generateTeams Server Action already exists and works. The page just needs to fetch the generated teams and display them.

    Mobile-first: keep max-w-4xl container, ensure drag-and-drop works on mobile (long-press 250ms already configured in TeamReveal).
  </action>
  <verify>
    <automated>grep -n "getMatchTeams" src/app/match/[id]/teams/page.tsx</automated>
  </verify>
  <done>
    Teams page fetches actual team data from database, displays them correctly, generate button and remélanger button both work
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply design system to balance indicator</name>
  <files>src/components/match/balance-indicator.tsx</files>
  <read_first>
    1. src/components/match/balance-indicator.tsx (current implementation)
    2. src/lib/design-tokens.ts (statusBadges, colors)
  </read_first>
  <action>
    Update src/components/match/balance-indicator.tsx to use the kickoff design system:
    1. Import statusBadges from '@/lib/design-tokens' (though this component calculates its own badges, so use the color palette)
    2. Replace hardcoded colors with kickoff palette:
       - For 'Équilibré': use bg-lime-glow background, text-lime-dark text (from statusBadges.confirmed)
       - For 'Léger avantage': use bg-yellow-card background, text-yellow-card text (from statusBadges.waitlisted)
       - For 'Déséquilibré': use bg-red-card background, text-red-card text (from statusBadges.no_show)
    3. Keep the same logic (normalizedDiff thresholds) but use kickoff color classes
    4. Use font-mono for the diff value (score display)
    5. Add FootballIcon name="ball" next to the label (small icon, 16x16)
    6. Remove the variant prop (we're using custom classes, not shadcn/ui variants)
    7. Keep the Badge wrapper but apply custom classes

    The normalizedDiff calculation and thresholds remain the same (0.15, 0.4).
  </action>
  <verify>
    <automated>grep -E "bg-lime-glow|bg-yellow-card|bg-red-card|FootballIcon" src/components/match/balance-indicator.tsx</automated>
  </verify>
  <done>
    Balance indicator uses kickoff design system colors and FootballIcon, displays correctly
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply design system to draggable player card</name>
  <files>src/components/match/draggable-player-card.tsx</files>
  <read_first>
    1. src/components/match/draggable-player-card.tsx (current implementation)
    2. src/lib/design-tokens.ts (colors, shadows)
    3. src/components/icons/football-icons.tsx (FootballIcon component)
  </read_first>
  <action>
    Update src/components/match/draggable-player-card.tsx to use the kickoff design system:
    1. Import FootballIcon from '@/components/icons/football-icons'
    2. Replace the avatar background colors with kickoff palette:
       - Team A: use bg-team-a background, text-team-a text (green #2D5016)
       - Team B: use bg-team-b background, text-team-b text (blue #3B82F6)
    3. Replace the border colors with kickoff palette:
       - Team A: border-l-team-a (same as bg-team-a)
       - Team B: border-l-team-b (same as bg-team-b)
    4. Replace shadow-md with shadow-card-hover from design tokens
    5. Use font-mono for the score display (e.g., "Score: 3.2")
    6. Replace the "Nouveau" text with a badge using statusBadges.confirmed (green "Confirmé" style) for new players
    7. Replace the drag handle SVG with FootballIcon name="jersey" (16x16, gray color)
    8. Add the rounded-card border-radius (16px) to the card

    Keep the touch-manipulation class and hover effects. The layout and functionality remain the same.
  </action>
  <verify>
    <automated>grep -E "FootballIcon|bg-team-a|bg-team-b|shadow-card-hover|rounded-card|font-mono" src/components/match/draggable-player-card.tsx</automated>
  </verify>
  <done>
    Player card uses kickoff design system colors, FootballIcon, and shadows, displays correctly
  </done>
</task>

<task type="auto">
  <name>Task 5: Apply design system to team reveal container</name>
  <files>src/components/match/team-reveal.tsx</files>
  <read_first>
    1. src/components/match/team-reveal.tsx (current implementation)
    2. src/lib/design-tokens.ts (colors, shadows)
  </read_first>
  <action>
    Update src/components/match/team-reveal.tsx to use the kickoff design system:
    1. Replace team header colors with kickoff palette:
       - Team A title: text-team-a instead of text-blue-700
       - Team B title: text-team-b instead of text-red-700
    2. Replace Card background with bg-chalk-pure (already white, but explicit)
    3. Use shadow-card instead of default Card shadow
    4. Use font-mono for the score displays (e.g., "Score: 42.3")
    5. Use rounded-card border-radius on the Card components
    6. Replace the grid gap-4 with the design system spacing (keep gap-4, it's consistent)
    7. Add FootballIcon name="jersey" next to each team header (small, 20x20)

    Keep the drag-and-drop functionality, touch sensor configuration, and optimistic UI updates. The structure and logic remain the same.
  </action>
  <verify>
    <automated>grep -E "text-team-a|text-team-b|shadow-card|rounded-card|font-mono|FootballIcon" src/components/match/team-reveal.tsx</automated>
  </verify>
  <done>
    Team reveal container uses kickoff design system colors and typography, displays correctly
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm test -- team-balancer` - all 25 tests pass
2. Run `pnpm typecheck` - no TypeScript errors
3. Run `pnpm build` - build succeeds without warnings
4. Create a test match with 10+ players, generate teams, verify:
   - Teams are generated and displayed
   - Balance indicator shows correct status (Équilibré / Léger avantage / Déséquilibré)
   - Player cards use correct colors (team A green, team B blue)
   - Drag-and-drop works on mobile (long-press)
   - Remélanger button regenerates teams
5. Check visual consistency: FootballIcon, statusBadges, colors from design-tokens.ts
</verification>

<success_criteria>
1. Organizer can generate balanced teams for a match with ≥4 confirmed players
2. Generated teams display with correct scores and player counts
3. Balance indicator accurately reflects team balance (normalized diff)
4. All UI components use kickoff design system (colors, icons, typography)
5. Drag-and-drop allows manual reassignment with optimistic UI updates
6. Remélanger button regenerates teams with different random combination
7. Tests pass, build succeeds, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-team-balancing/04-01-SUMMARY.md`
</output>
