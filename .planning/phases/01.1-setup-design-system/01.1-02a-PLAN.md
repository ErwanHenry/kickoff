# Plan: Retrofit Colors & Backgrounds

**Phase:** 01.1 - Setup Design System
**Created:** 2026-03-31
**Status:** complete
**Depends on:** 01.1-01-PLAN.md (Setup Design System Foundation)

---

## Objective

Replace all hardcoded hex colors and generic backgrounds with kickoff design system colors (pitch, lime, chalk, etc.) across existing components.

**Context:** Design system tokens are now available. This phase retrofits existing UI to use the new color palette instead of hardcoded values.

---

## Wave 1: Find & Audit Hardcoded Colors

<task>
<id>01.1-02a-audit-colors</id>
<dependencies>01.1-04-design-tokens</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src</src>
</files>

<action>
Audit all `.tsx` and `.css` files for hardcoded hex colors.

1. Run grep to find all hardcoded hex colors:
```bash
grep -r "#[0-9A-Fa-f]{6}" src/ --include="*.tsx" --include="*.css" -n
```

2. Document findings in a comment block:
- List each file with hardcoded colors
- Note the context (button, badge, background, etc.)
- Identify the replacement Tailwind class

3. Create a replacement mapping:
```
#2D5016 → text-pitch or bg-pitch
#4ADE80 → text-lime or bg-lime
#F8FAF5 → bg-chalk
#FFFFFF → bg-chalk-pure
#EF4444 → text-red-card or bg-red-card
#FACC15 → text-yellow-card or bg-yellow-card
#3B82F6 → text-whistle-blue or bg-whistle-blue
```
</action>

<verify>
- All files with hardcoded colors are documented
- Replacement mapping is clear for each hex code
- No hex colors are missed in the audit
</verify>

<done>
Complete audit of hardcoded colors across the codebase with replacement mapping.
</done>
</task>

---

## Wave 2: Replace Background Colors

<task>
<id>01.1-02a-replace-backgrounds</id>
<dependencies>01.1-02a-audit-colors</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/(dashboard)/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/m/[shareToken]/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/(dashboard)/matches/new/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/match/[id]/teams/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/match/[id]/attendance/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/match-card.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/player-list.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/rsvp-button.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/match-form.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/team-reveal.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/draggable-player-card.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/attendance-form.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/install-prompt.tsx</src>
</files>

<action>
Replace hardcoded background colors with Tailwind chalk classes.

**Global Background (already done in layout.tsx):**
- `body` has `bg-chalk` applied

**Component Replacements:**

1. **Cards** (all Card components):
   - Replace `bg-white` or `bg-background` with `bg-chalk-pure`
   - Add `shadow-card` class

2. **MatchCard** (`src/components/match/match-card.tsx`):
   - Add `shadow-card hover:shadow-card-hover` to the Card component

3. **Forms** (`match-form.tsx`, `login-form.tsx`, `register-form.tsx`, `magic-link-form.tsx`):
   - Replace any hardcoded `bg-` classes with `bg-chalk-pure`

4. **RSVP Button** (`rsvp-button.tsx`):
   - Replace primary button background with `bg-pitch hover:bg-pitch-light`
   - Replace accent button background with `bg-lime hover:bg-lime-glow`

5. **Player Cards** (`player-list.tsx`, `draggable-player-card.tsx`):
   - Use `bg-chalk-pure shadow-card`

6. **Team Reveal** (`team-reveal.tsx`):
   - Team A cards: `bg-chalk-pure border-pitch/20`
   - Team B cards: `bg-chalk-pure border-whistle-blue/20`

7. **Install Prompt** (`install-prompt.tsx`):
   - Use `bg-chalk-pure shadow-card`

**Import statement to add where needed:**
```typescript
// No imports needed - using Tailwind classes
```
</action>

<verify>
- `pnpm build` succeeds
- `pnpm typecheck` passes
- No hardcoded hex colors remain in className strings
- All cards use `bg-chalk-pure` with `shadow-card`
- App background is `bg-chalk` globally
- Visual regression check: pages look correct with new backgrounds
</verify>

<done>
All background colors replaced with kickoff design system chalk palette.
</done>
</task>

---

## Wave 3: Replace Text & Accent Colors

<task>
<id>01.1-02a-replace-text-colors</id>
<dependencies>01.1-02a-replace-backgrounds</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/match-card.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/rsvp-button.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/player-list.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/team-reveal.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/balance-indicator.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/install-prompt.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/(dashboard)/page.tsx</src>
</files>

<action>
Replace hardcoded text colors with kickoff semantic color classes.

**Text Color Replacements:**

1. **Primary Text (headings, CTAs):**
   - Replace `text-primary` or hardcoded dark colors with `text-pitch`

2. **Accent Text (success, confirmations):**
   - Replace green colors with `text-lime` or `text-lime-dark`

3. **Links & Info:**
   - Replace blue colors with `text-whistle-blue`

4. **Destructive (errors, no-show):**
   - Replace red colors with `text-red-card`

5. **Warning (waitlist, attention):**
   - Replace yellow colors with `text-yellow-card`

**Specific Component Updates:**

1. **MatchCard** (`match-card.tsx`):
   - Status "Open": `text-pitch` (already uses variant prop)
   - Status "Full": `text-yellow-card`
   - Status "Confirmed": `text-lime-dark`

2. **RSVP Button** (`rsvp-button.tsx`):
   - Primary state: `text-chalk-pure` (on `bg-pitch`)
   - Confirmed state: `text-lime-dark` (on `bg-lime-glow`)

3. **Balance Indicator** (`balance-indicator.tsx`):
   - Balanced: `text-lime`
   - Slight advantage: `text-yellow-card`
   - Disadvantaged: `text-red-card`

4. **Team Reveal** (`team-reveal.tsx`):
   - Team A label: `text-pitch`
   - Team B label: `text-whistle-blue`

5. **Install Prompt** (`install-prompt.tsx`):
   - CTA text: `text-pitch`

**Import statement to add where needed:**
```typescript
// No imports needed - using Tailwind classes
```
</action>

<verify>
- `pnpm build` succeeds
- `pnpm typecheck` passes
- No hardcoded hex colors in text classes
- Semantic colors match design system
- Visual check: text is readable and matches intent
</verify>

<done>
All text and accent colors replaced with kickoff semantic color palette.
</done>
</task>

---

## Wave 4: Replace Border & Shadow Colors

<task>
<id>01.1-02a-replace-borders-shadows</id>
<dependencies>01.1-02a-replace-text-colors</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/team-reveal.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/draggable-player-card.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/player-list.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/match-form.tsx</src>
</files>

<action>
Replace hardcoded border and shadow colors with kickoff design system tokens.

**Border Replacements:**

1. **Team Borders** (`team-reveal.tsx`):
   - Team A: `border-pitch/20` or `border-pitch`
   - Team B: `border-whistle-blue/20` or `border-whistle-blue`

2. **Form Inputs** (`match-form.tsx`, `login-form.tsx`, etc.):
   - Focus border: `focus:ring-pitch`
   - Default border: `border-border` (shadcn default)

3. **Player Cards** (`draggable-player-card.tsx`):
   - Default: `border-border`
   - Dragging: `border-pitch`

**Shadow Replacements:**

1. **All Cards:**
   - Replace `shadow-sm`, `shadow-md`, etc. with `shadow-card`
   - Hover state: `hover:shadow-card-hover`

2. **Buttons:**
   - Hover state: `hover:shadow-btn-hover`

3. **Elevated Elements:**
   - Use `shadow-card` for consistent elevation

**Import statement to add where needed:**
```typescript
// No imports needed - using Tailwind classes
```
</action>

<verify>
- `pnpm build` succeeds
- `pnpm typecheck` passes
- No hardcoded shadow values
- Borders use semantic pitch/whistle-blue colors
- Shadows use kickoff card/btn variants
</verify>

<done>
All borders and shadows use kickoff design system tokens.
</done>
</task>

---

## Wave 5: Final Color Audit

<task>
<id>01.1-02a-final-audit</id>
<dependencies>01.1-02a-replace-borders-shadows</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src</src>
</files>

<action>
Final verification that no hardcoded colors remain.

1. Run grep again to ensure no hex colors:
```bash
grep -r "#[0-9A-Fa-f]{6}" src/ --include="*.tsx" --include="*.css" -n
```

2. Verify any remaining matches are:
   - In comments (OK)
   - In external library configs (OK)
   - In test files (OK)

3. Test visual appearance:
   - Run `pnpm dev`
   - Check each page visually
   - Ensure colors match design system

4. Check specific pages:
   - Landing page (`/`)
   - Dashboard (`/dashboard`)
   - Match creation (`/dashboard/matches/new`)
   - Public match page (`/m/[token]`)
   - Teams page (`/match/[id]/teams`)
</action>

<verify>
- No hardcoded hex colors in component files (excluding comments)
- All pages render with correct kickoff colors
- `pnpm build` passes
- `pnpm typecheck` passes
- Visual regression: pages look correct
</verify>

<done>
Complete color retrofit — no hardcoded colors remain in the codebase.
</done>
</task>

---

## Success Criteria

- [ ] No hardcoded hex colors in component files (excluding comments/tests)
- [ ] All backgrounds use `bg-chalk` or `bg-chalk-pure`
- [ ] All cards use `shadow-card`
- [ ] Primary text uses `text-pitch`
- [ ] Success states use `text-lime` / `bg-lime-glow`
- [ ] Errors use `text-red-card` / `bg-red-card`
- [ ] Warnings use `text-yellow-card` / `bg-yellow-card`
- [ ] Team A uses `text-pitch` / `border-pitch`
- [ ] Team B uses `text-whistle-blue` / `border-whistle-blue`
- [ ] `pnpm build` passes
- [ ] `pnpm typecheck` passes

---

## Notes

- **Preserve functionality** — only change colors, not behavior
- **Test visually** — color changes need manual verification
- **shadcn/ui components** use CSS variables, respect those (don't override)
- **Next step** — Phase 01.1-02b will replace icons and typography
