---
phase: 08-groups-leaderboards
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/lib/actions/groups.ts
  - src/lib/db/queries/groups.ts
  - src/app/(dashboard)/groups/page.tsx
  - src/components/group/groups-dashboard.tsx
  - src/components/group/join-group-form.tsx
  - src/components/group/group-card.tsx
  - src/components/layout/mobile-nav.tsx
autonomous: false
requirements:
  - GROUP-03
  - GROUP-04
  - MATCH-08

must_haves:
  truths:
    - "Organisateur peut générer lien d'invitation avec code"
    - "Joueur peut rejoindre un groupe via code d'invitation"
    - "Dashboard groupes liste tous les groupes de l'utilisateur"
    - "Chaque carte groupe montre: nom, nombre membres, rôle utilisateur"
    - "Match peut être associé à un groupe (optionnel)"
    - "Invitation fonctionne pour users connectés"
  artifacts:
    - path: "src/lib/actions/groups.ts"
      provides: "joinGroup Server Action"
      exports: ["joinGroup"]
    - path: "src/app/(dashboard)/groups/page.tsx"
      provides: "Groups dashboard page"
      min_lines: 40
    - path: "src/components/group/groups-dashboard.tsx"
      provides: "Groups list with create/join UI"
      min_lines: 50
    - path: "src/components/group/join-group-form.tsx"
      provides: "Join form with invite code input"
      min_lines: 40
    - path: "src/components/group/group-card.tsx"
      provides: "Group card component"
      min_lines: 40
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Updated with Groups link"
      min_lines: 20
  key_links:
    - from: "src/components/group/join-group-form.tsx"
      to: "joinGroup action"
      via: "joinGroup({ inviteCode }) call"
      pattern: "joinGroup\\(\\{.*inviteCode"
    - from: "src/app/(dashboard)/groups/page.tsx"
      to: "getUserGroups query"
      via: "await getUserGroups(userId)"
      pattern: "getUserGroups\\(userId\\)"
    - from: "src/components/match/match-form.tsx"
      to: "groups table"
      via: "group select option"
      pattern: "groupId.*groups"
---

<objective>
Implement group joining via invite codes and create the groups dashboard. Users can join groups with invite codes, and organizers can associate matches with groups during creation.

Purpose: Enable group discovery and joining, while providing a centralized dashboard for managing all groups.
Output: Working group join flow and groups dashboard with group cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-groups-leaderboards/08-CONTEXT.md
@.planning/phases/08-groups-leaderboards/08-01-SUMMARY.md
@.planning/phases/08-groups-leaderboards/08-02-SUMMARY.md

@src/db/schema.ts
@src/lib/design-tokens.ts
@src/lib/actions/groups.ts
@src/components/match/match-form.tsx
@src/app/(dashboard)/page.tsx
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/lib/actions/groups.ts (created in 08-01):
```typescript
export interface CreateGroupResult {
  id: string;
  slug: string;
  inviteCode: string;
}

export async function createGroup(input: { name: string, slug?: string })
  => Promise<{ success: true; data: CreateGroupResult } | { error: string }>
```

From src/db/schema.ts:
```typescript
// group_members role enum:
role: 'captain' | 'manager' | 'player'

// groups table for invite code validation:
{
  inviteCode: string; // unique
  slug: string; // unique
}
```

From src/lib/design-tokens.ts:
```typescript
// Domain icon for groups:
FootballIcon name="cornerFlag"  // Use for groups

// Status badge pattern:
statusBadges.confirmed // Example structure
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add joinGroup and invite validation queries/actions</name>
  <files>src/lib/db/queries/groups.ts, src/lib/actions/groups.ts</files>
  <read_first>
    - src/lib/db/queries/groups.ts (existing queries)
    - src/lib/actions/groups.ts (existing createGroup)
    - src/lib/actions/rsvp.ts (for action pattern with transactions)
  </read_first>
  <action>
Extend src/lib/db/queries/groups.ts:

1. **getGroupByInviteCode(code: string)** - Fetch group by invite code
   - Select from groups where inviteCode equals code
   - Returns: { id, name, slug, inviteCode } or null if not found
   - Use for validation before joining

Extend src/lib/actions/groups.ts:

1. **joinGroup(input: { inviteCode: string })** - Join a group via invite code
   - Use 'use server' directive
   - Get session via auth.api.getSession({ headers: new Headers() })
   - Return { error: 'Non authentifié' } if no session
   - Validate inviteCode format: z.string().min(6).max(6)
   - Call getGroupByInviteCode(inviteCode) to verify code exists
   - Return { error: 'Code d\'invitation invalide' } if group not found
   - Check if user is already a member:
     - Query group_members where userId equals session.user.id AND groupId equals group.id
     - Return { error: 'Tu es déjà membre de ce groupe' } if exists
   - Insert into group_members:
     - groupId: group.id
     - userId: session.user.id
     - role: 'player' (default role for joined members)
     - joinedAt: new Date()
   - Initialize player_stats for this user+group combination:
     - Insert into player_stats if not exists:
       - userId: session.user.id
       - groupId: group.id
       - All other fields: default values (0 for counts, 3.0 for averages)
     - Use ON CONFLICT DO NOTHING to handle existing stats
   - Revalidate paths: revalidatePath('/dashboard/groups'), revalidatePath(`/group/${group.slug}`)
   - Return { success: true, data: { slug: group.slug, name: group.name } }
   - Catch errors and return { error: 'Erreur lors de la jonction de groupe' }

Follow the transaction and error handling pattern from rsvp.ts.
  </action>
  <verify>
    <automated>grep -n "export.*getGroupByInviteCode\|export.*joinGroup" src/lib/db/queries/groups.ts src/lib/actions/groups.ts | wc -l | xargs -I {} test {} -ge 2</automated>
    <automated>grep -n "role: 'player'\|getGroupByInviteCode\|player_stats.*groupId" src/lib/actions/groups.ts | wc -l | xargs -I {} test {} -ge 3</automated>
  </verify>
  <done>
joinGroup Server Action exists with invite code validation, role assignment, and stats initialization.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update match form to include group selection</name>
  <files>src/components/match/match-form.tsx</files>
  <read_first>
    - src/components/match/match-form.tsx (existing form)
    - src/lib/db/queries/groups.ts (getUserGroups query)
  </read_first>
  <action>
Update src/components/match/match-form.tsx to add group selection:

1. Fetch user's groups on component mount:
   - Add useEffect to call getUserGroups(session.user.id)
   - Store in state: groups: UserGroup[]

2. Add group select field (after max_players, before recurrence):
   - Label: "Groupe (optionnel)"
   - Description: "Associe ce match à un groupe pour le classement"
   - Use: <Select name="groupId" value={groupId} onValueChange={setGroupId}>
   - Options:
     - First option: value="", label="Aucun groupe"
     - Map groups to options: value={group.id}, label={group.name}
   - Show member count in option: "{group.name} ({group.memberCount} membres)"
   - If no groups, show disabled state with message "Crée un groupe d'abord"

3. Include groupId in form submission:
   - Pass groupId to createMatch action
   - Use undefined if groupId is empty string

4. Styling:
   - Use shadcn/ui Select component
   - Mobile-first: Full width on mobile
   - Show FootballIcon name="cornerFlag" in label

Follow the existing form pattern in match-form.tsx for consistent styling.
  </action>
  <verify>
    <automated>grep -n "groupId\|Groupe.*optionnel\|getUserGroups\|Select.*name=.*groupId" src/components/match/match-form.tsx | wc -l | xargs -I {} test {} -ge 4</automated>
  </verify>
  <done>
Match form has group selection dropdown with user's groups. Match can be associated with group.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create join group form component</name>
  <files>src/components/group/join-group-form.tsx</files>
  <read_first>
    - src/components/group/group-form.tsx (for form pattern)
    - src/lib/design-tokens.ts (for colors and icons)
    - src/lib/validations/group.ts (for validation schema reference)
  </read_first>
  <action>
Create src/components/group/join-group-form.tsx as a client component ("use client"):

Props: { onSuccess?: (group: { slug: string, name: string }) => void }

UI Structure (mobile-first, max-w-md):
1. Card with bg-chalk-pure, shadow-card, rounded-card
2. Header: "Rejoindre un groupe" (font-sans, text-xl, font-semibold)
3. Description: "Entre le code d'invitation à 6 caractères" (text-sm, text-slate-mid)
4. Form fields:
   - **Invite code input** (from shadcn/ui Input):
     - Label: "Code d'invitation"
     - Placeholder: "ABC123"
     - Required: true
     - Uppercase: transform input to uppercase automatically
     - MaxLength: 6
     - Center text: text-center, font-mono, text-lg, tracking-widest
     - Use: <Input id="inviteCode" name="inviteCode" required maxLength={6} uppercase />
5. Submit button:
   - Text: "Rejoindre"
   - Variant: default (bg-pitch, text-white)
   - Full width
   - Disabled while submitting
   - Use FootballIcon name="boot" (players/RSVP icon) for joining action
6. Error message: Display action.error in red (text-red-card) if present
7. Loading state: Show "Recherche..." on button while submitting

State Management:
- inviteCode: string state for input (uppercase)
- isSubmitting: boolean state
- Validate format: 6 chars, alphanumeric

Form Submission:
- Call joinGroup({ inviteCode }) on form submit
- Handle success: call onSuccess(data) if provided, redirect to /group/{slug}
- Handle error: display error message
- Use toast from sonner for success/error feedback

Styling Details:
- Input should be prominent (large text, centered)
- Auto-focus on input field on mount
- Submit button disabled if code length !== 6

Follow the mobile-first pattern from group-form.tsx with prominent input field.
  </action>
  <verify>
    <automated>grep -n "use client\|Code d'invitation\|joinGroup\|ABC123\|toUpperCase\|maxLength.*6" src/components/group/join-group-form.tsx | wc -l | xargs -I {} test {} -ge 6</automated>
  </verify>
  <done>
Join group form exists with 6-char uppercase input, validation, and error handling.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create group card component</name>
  <files>src/components/group/group-card.tsx</files>
  <read_first>
    - src/components/match/match-card.tsx (for card pattern)
    - src/lib/design-tokens.ts (for colors)
    - src/components/ui/badge.tsx (for role badge)
  </read_first>
  <action>
Create src/components/group/group-card.tsx as a Server Component:

Props: { group: GroupCardData }

Type Definition:
```typescript
type GroupCardData = {
  id: string;
  name: string;
  slug: string;
  memberCount: number;
  role: 'captain' | 'manager' | 'player';
  createdAt: Date;
  inviteCode: string;
}
```

UI Structure (card layout, hover effect):
1. Card base:
   - bg-chalk-pure, shadow-card, rounded-card, rounded-card
   - Hover: shadow-card-hover, scale-[1.02] transition
   - Cursor: pointer
   - Click: Navigates to /group/{slug}
2. Card content (p-4):
   - **Header row** (flex, justify-between, items-center):
     a. Group name: font-sans, font-semibold, text-lg, truncate
     b. Role badge (small):
        - Captain: bg-lime-glow, text-lime-dark, label "Capitaine"
        - Manager: bg-blue-100, text-blue-800, label "Manager"
        - Player: bg-slate-100, text-slate-600, label "Joueur"
   - **Meta row** (flex, items-center, gap-4, mt-2):
     a. Member count: text-slate-mid, text-sm, font-mono
        - Icon: FootballIcon name="boot" (players)
        - Text: "{memberCount} membres"
     b. Created date: text-slate-light, text-xs, font-mono
        - Text: "depuis {month year}" using date-fns format
   - **Action row** (mt-3, pt-3, border-t, border-slate-lighter):
     - "Voir le groupe" link: text-whistle-blue, text-sm, font-medium
     - ChevronRight icon from lucide-react

Styling Rules:
- Use font-mono for member count and dates
- Use font-sans for group name
- Use FootballIcon name="cornerFlag" if you add an icon
- Mobile: Full width, proper padding for touch targets

Follow the card pattern from match-card.tsx with hover state and role badge.
  </action>
  <verify>
    <automated>grep -n "membres\|Voir le groupe\|role.*captain.*manager.*player\|href.*group" src/components/group/group-card.tsx | wc -l | xargs -I {} test {} -ge 4</automated>
  </verify>
  <done>
Group card component displays name, member count, role badge, and navigation link.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Group joining infrastructure with validation, match form integration with group selection, join form component, and group cards.</what-built>
  <how-to-verify>
1. Run `pnpm build` to verify TypeScript compilation
2. Run `pnpm typecheck` to verify no type errors
3. Check joinGroup action:
   - `grep -n "role: 'player'\|getGroupByInviteCode" src/lib/actions/groups.ts`
4. Check match form group selection:
   - `grep -n "groupId\|Groupe.*optionnel\|getUserGroups" src/components/match/match-form.tsx`
5. Check join form:
   - `grep -n "Code d'invitation\|ABC123\|maxLength.*6" src/components/group/join-group-form.tsx`
6. Check group card:
   - `grep -n "membres\|Capitaine\|href.*group" src/components/group/group-card.tsx`

Expected output:
- No build errors
- No type errors
- joinGroup validates invite codes
- Match form has group dropdown
- Join form has 6-char input
- Group card has role badge
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to groups dashboard, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Create groups dashboard page</name>
  <files>src/app/(dashboard)/groups/page.tsx, src/components/group/groups-dashboard.tsx</files>
  <read_first>
    - src/app/(dashboard)/page.tsx (for dashboard pattern)
    - src/app/(dashboard)/groups/new/page.tsx (for new group link)
    - src/lib/db/queries/groups.ts (getUserGroups query)
  </read_first>
  <action>
Create src/app/(dashboard)/groups/page.tsx as a Server Component:

Page Metadata:
- title: "Mes groupes - kickoff"
- description: "Gère tes groupes de foot"

Data Fetching:
1. Get session: auth.api.getSession({ headers: await headers() })
2. If not authenticated, redirect to /login
3. Fetch groups: getUserGroups(session.user.id)
4. Separate by role: captainGroups (role === 'captain'), otherGroups (role !== 'captain')

Page Layout (max-w-4xl, mx-auto, px-4 py-8):
1. **Header section**:
   - Page title: "Mes groupes" (h1, font-sans, text-2xl, font-semibold)
   - Description: "Crée et rejoins des groupes pour organiser des matchs" (text-slate-mid, text-sm)
   - Action buttons row (flex, gap-3, mt-4):
     a. "Créer un groupe" button: Link to /dashboard/groups/new, bg-pitch, text-white
     b. "Rejoindre un groupe" button: Opens join dialog, bg-chalk-pure, border, text-pitch
2. **Groups section**:
   - If no groups: Empty state with "Aucun groupe" message and CTA buttons
   - If groups exist:
     a. "Mes groupes" section (if captainGroups.length > 0):
        - Heading: "Mes groupes" (font-sans, text-lg, font-semibold, mt-6)
        - Grid: Grid layout, cols-1 on mobile, cols-2 on md, gap-4
        - Render <GroupCard> for each captainGroups
     b. "Autres groupes" section (if otherGroups.length > 0):
        - Heading: "Autres groupes" (font-sans, text-lg, font-semibold, mt-6)
        - Grid: Same grid layout
        - Render <GroupCard> for each otherGroups

Create src/components/group/groups-dashboard.tsx as the main dashboard component:

Props: { userGroups: UserGroup[] }

UI Structure:
1. Same layout as described above
2. Join dialog: Use shadcn/ui Dialog with <JoinGroupForm />
3. Handle join success: redirect to /group/{slug}

State Management (client part):
- isJoinDialogOpen: boolean for dialog state
- Handle join success: close dialog, redirect or show success

Follow the dashboard pattern from (dashboard)/page.tsx with empty states and CTAs.
  </action>
  <verify>
    <automated>grep -n "Mes groupes\|getUserGroups\|GroupCard\|JoinGroupForm\|Créer un groupe\|Rejoindre un groupe" src/app/(dashboard)/groups/page.tsx src/components/group/groups-dashboard.tsx | wc -l | xargs -I {} test {} -ge 6</automated>
  </verify>
  <done>
Groups dashboard page exists with create/join actions and group cards display.
  </done>
</task>

<task type="auto">
  <name>Task 6: Update mobile navigation with Groups link</name>
  <files>src/components/layout/mobile-nav.tsx</files>
  <read_first>
    - src/components/layout/mobile-nav.tsx (existing nav)
    - src/lib/design-tokens.ts (for icons)
  </read_first>
  <action>
Update src/components/layout/mobile-nav.tsx to add Groups tab:

1. Add new navigation item after "Matchs" and before "Profil":
   - Label: "Groupes"
   - href: /dashboard/groups
   - Icon: FootballIcon name="cornerFlag" (groups icon)
   - Active state: Check if pathname starts with /dashboard/groups

2. Update navigation items array:
```typescript
const navItems = [
  { href: '/dashboard', label: 'Accueil', icon: 'pitch' },
  { href: '/dashboard/matches', label: 'Matchs', icon: 'centerCircle' },
  { href: '/dashboard/groups', label: 'Groupes', icon: 'cornerFlag' },  // NEW
  { href: '/profile', label: 'Profil', icon: 'star' },
];
```

3. Update active checking logic to include /dashboard/groups

Follow the existing nav pattern with proper active states and icons.
  </action>
  <verify>
    <automated>grep -n "Groupes\|dashboard/groups\|cornerFlag" src/components/layout/mobile-nav.tsx | wc -l | xargs -I {} test {} -ge 3</automated>
  </verify>
  <done>
Mobile navigation has Groups tab with correct icon and link.
  </done>
</task>

</tasks>

<verification>
1. joinGroup action validates invite codes (6 chars, alphanumeric)
2. Joining creates group_members entry with role='player'
3. Joining initializes player_stats for the user+group
4. Match form has group dropdown with user's groups
5. Match can be associated with group (groupId in matches table)
6. Join form has uppercase 6-char input with validation
7. Group card shows role badge and member count
8. Groups dashboard shows user's groups separated by role
9. Create/join buttons work correctly
10. Mobile nav has Groups link
</verification>

<success_criteria>
1. User can join group via invite code
2. Joining sets player role and initializes stats
3. Match creation form includes group selection
4. Groups dashboard shows all user's groups
5. Group cards display name, members, role
6. Create/join flows work smoothly
7. Mobile navigation updated
8. No TypeScript errors
9. Mobile-optimized layout
10. Empty states display correctly
</success_criteria>

<output>
After completion, create `.planning/phases/08-groups-leaderboards/08-03-SUMMARY.md` with:
- Group joining flow confirmed
- Match-group association working
- Dashboard navigation complete
- Mobile nav updated
- Any UX improvements made
</output>
