---
phase: 02-match-creation-guest-rsvp
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/app/api/waitlist/route.ts
  - src/app/api/matches/[id]/route.ts
  - src/components/match/match-card.tsx
  - src/app/(dashboard)/page.tsx
autonomous: true
requirements:
  - WAIT-01
  - MATCH-07
must_haves:
  truths:
    - When confirmed player cancels, first waitlisted player auto-promotes
    - Organizer sees upcoming match with confirmed count on dashboard
    - Organizer can navigate to recent matches and create new matches from dashboard
  artifacts:
    - path: src/app/api/waitlist/route.ts
      provides: Server Action for waitlist promotion
      exports: ["promoteFirstWaitlisted"]
    - path: src/app/api/matches/[id]/route.ts
      provides: Server Action for single match queries
      exports: ["getMatchById"]
    - path: src/components/match/match-card.tsx
      provides: Compact card component for match lists
      min_lines: 80
    - path: src/app/(dashboard)/page.tsx
      provides: Dashboard page with upcoming/recent matches
      min_lines: 150
  key_links:
    - from: src/app/api/waitlist/route.ts
      to: src/app/api/rsvp/route.ts
      via: call on cancellation
      pattern: promoteFirstWaitlisted\\(.*
    - from: src/app/(dashboard)/page.tsx
      to: src/app/api/matches/[id]/route.ts
      via: getMatchById()
      pattern: getMatchById\\(.*
    - from: src/app/(dashboard)/page.tsx
      to: src/components/match/match-card.tsx
      via: component render
      pattern: MatchCard
---

<objective>
Implement automatic waitlist promotion and create the organizer dashboard — completing the waitlist management flow and providing a central hub for match management.

Purpose: When a confirmed player cancels, the first waitlisted player must be automatically promoted (WAIT-01). This requires an integrated flow between cancellation and promotion logic. Simultaneously, the organizer needs a dashboard to view upcoming/recent matches (MATCH-07) with quick access to match details and creation.

Output: Working waitlist promotion system and dashboard page with upcoming match card, recent matches list, and navigation actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-match-creation-guest-rsvp/02-CONTEXT.md
@.planning/phases/02-match-creation-guest-rsvp/02-RESEARCH.md
@.planning/phases/02-match-creation-guest-rsvp/02-UI-SPEC.md
@src/db/schema.ts
@src/lib/auth.ts
@src/lib/db/queries/matches.ts
@src/app/api/rsvp/route.ts
</context>

<interfaces>
<!-- Key types from prior plans -->
From src/db/schema.ts:
```typescript
export type Match = typeof matches.$inferSelect;
export type MatchPlayer = typeof matchPlayers.$inferSelect;

// Waitlist promotion requires:
// - Finding first waitlisted player (earliest confirmedAt)
// - Updating status to "confirmed"
// - Updating match status if now full
// - Sending email notification (if user has account) - Phase 10
```

From src/app/api/rsvp/route.ts (created in 02-02):
```typescript
// cancelRsvp() already exists and updates player status
// We need to integrate promotion logic here
export async function cancelRsvp(formData: FormData) {
  // ... existing cancellation logic ...
  // Need to add: call promoteFirstWaitlisted() after cancellation
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create waitlist promotion Server Action</name>
  <files>src/app/api/waitlist/route.ts</files>
  <read_first>src/db/schema.ts, src/app/api/rsvp/route.ts</read_first>
  <action>
Create src/app/api/waitlist/route.ts with:

1. Add "use server" directive
2. Import: db, matchPlayers, matches schema, eq, sql, asc from drizzle-orm, revalidatePath
3. Export async function promoteFirstWaitlisted(matchId: string):
   - **CRITICAL: Use transaction to prevent race conditions**
   - **COORDINATION NOTE: Must account for concurrent RSVP operations from 02-02**
     * Both 02-02 (rsvpMatch) and 02-03 (promoteFirstWaitlisted) modify match.status
     * Use FOR UPDATE locking on match row to serialize access
     * Transaction must re-check confirmed count after acquiring lock
   - In transaction:
     * Lock match row: select matches where id=matchId for update
     * Find first waitlisted player:
       - Query match_players where matchId AND status='waitlisted'
       - Order by confirmedAt ASC (earliest waitlist joins first)
       - Limit 1
     * If no waitlisted players, return early
     * Update player.status from "waitlisted" to "confirmed"
     * Update player.confirmedAt to now()
     * Check if match is now full:
       - Count confirmed players (AFTER acquiring lock, re-check)
       - If count >= match.maxPlayers, update match.status to "full"
   - RevalidatePath(`/m/${match.shareToken}`) (need to fetch match for shareToken)
   - Return { success: true, promotedPlayer: { id, guestName, newPosition } }
   - **Email notification deferred to Phase 10** (WAIT-02)

4. Export async function getWaitlistPosition(matchId: string, guestToken: string):
   - Count waitlisted players with confirmedAt < this player's confirmedAt
   - Return position + 1 (1-indexed)

Per RESEARCH.md waitlist logic and CONTEXT.md requirements for automatic promotion.

**IMPORTANT CONCURRENCY COORDINATION:**
This plan (02-03) and plan 02-02 both modify match.status. The promotion transaction must:
1. Use FOR UPDATE locking on the match row (same as 02-02 rsvpMatch)
2. Re-check confirmed count after acquiring lock (concurrent RSVP may have changed it)
3. Serialize all match.status updates to prevent lost updates
  </action>
  <verify>
    <automated>grep -q "export.*function promoteFirstWaitlisted" src/app/api/waitlist/route.ts && grep -q "db.transaction" src/app/api/waitlist/route.ts && grep -q "order by confirmed_at" src/app/api/waitlist/route.ts && grep -q "for update" src/app/api/waitlist/route.ts</automated>
  </verify>
  <done>
    - promoteFirstWaitlisted() promotes first waitlisted player using transaction
    - FOR UPDATE locking serializes access with concurrent RSVP operations (02-02)
    - Transaction re-checks confirmed count after lock to account for race conditions
    - Waitlist ordered by confirmedAt ASC (first come, first served)
    - Match status updated to "full" if now full
    - Revalidation of public match page
    - Email notification hook added (implementation Phase 10)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrate promotion into cancellation flow</name>
  <files>src/app/api/rsvp/route.ts</files>
  <read_first>src/app/api/waitlist/route.ts, src/app/api/rsvp/route.ts</read_first>
  <action>
Modify src/app/api/rsvp/route.ts:

1. Import promoteFirstWaitlisted from @/app/api/waitlist/route
2. In cancelRsvp() function:
   - After updating player.status to "cancelled"
   - Add logic:
     * Check if match has waitlisted players (query count)
     * If yes, call await promoteFirstWaitlisted(matchId)
     * If no waitlisted players and match was "full", change status to "open"
   - Update existing WAIT-03 logic to use promotion instead of just status change

Per REQUIREMENTS.md WAIT-01: "When confirmed player cancels, first waitlisted player auto-promotes"
  </action>
  <verify>
    <automated>grep -q "promoteFirstWaitlisted" src/app/api/rsvp/route.ts</automated>
  </verify>
  <done>
    - cancelRsvp() now calls promoteFirstWaitlisted() when waitlist exists
    - Match status correctly transitions: full → open (when promotion happens)
    - Integration tested with transaction safety
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create match card component</name>
  <files>src/components/match/match-card.tsx</files>
  <read_first>src/components/ui/card.tsx, src/components/ui/badge.tsx</read_first>
  <action>
Create src/components/match/match-card.tsx as Server Component:

1. Define props interface:
   - match: Match with date, location, title, status, maxPlayers
   - confirmedCount: number
   - shareToken: string
   - variant: "upcoming" | "recent" (affects layout priority)
2. Render compact Card with:
   - Status badge (colored per status):
     * "Ouvert" → green (accent)
     * "Complet" → red (destructive)
     * "Verrouillé" → gray (muted)
   - Date/time display: "Mar 15 avril 20h" (formatted with date-fns or native Intl)
   - Location text with icon
   - Player count: "{confirmed}/{max} confirmés"
   - For upcoming: link to /m/{shareToken} (public page)
   - For recent: link to /match/{id} (detail page - Phase 5)
3. Use mobile-first layout (stack vertically on small screens)
4. Add hover effect (scale-105 on desktop)

Per 02-UI-SPEC.md general card patterns and CONTEXT.md D-19 for status colors.
  </action>
  <verify>
    <automated>grep -q "Ouvert" src/components/match/match-card.tsx && grep -q "Complet" src/components/match/match-card.tsx && grep -q "Verrouillé" src/components/match/match-card.tsx && grep -q "confirmés" src/components/match/match-card.tsx</automated>
  </verify>
  <done>
    - MatchCard component displays match summary
    - Status badge with correct colors
    - Date/time and location formatted
    - Player count shown
    - Links to appropriate page based on variant
    - Mobile-first responsive layout
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create match detail query API</name>
  <files>src/app/api/matches/[id]/route.ts</files>
  <read_first>src/lib/db/queries/matches.ts, src/db/schema.ts</read_first>
  <action>
Create src/app/api/matches/[id]/route.ts with:

1. Add "use server" directive
2. Import: db, matches schema, eq from drizzle-orm, notFound from next/navigation
3. Export async function getMatchById(id: string):
   - Query matches table where id matches (uuid validation)
   - Return match or throw notFound() if not found
4. Export async function getMatchWithPlayers(id: string):
   - Get match by id
   - Get players via getMatchPlayers(id)
   - Get confirmed and waitlist counts
   - Return { match, players, confirmedCount, waitlistCount }

Per RESEARCH.md query patterns for dashboard data fetching.
  </action>
  <verify>
    <automated>grep -q "export.*function getMatchById" src/app/api/matches/\\[id\\]/route.ts && grep -q "export.*function getMatchWithPlayers" src/app/api/matches/\\[id\\]/route.ts</automated>
  </verify>
  <done>
    - getMatchById() retrieves single match by UUID
    - getMatchWithPlayers() retrieves match with player data
    - Proper 404 handling for invalid IDs
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create dashboard page</name>
  <files>src/app/(dashboard)/page.tsx</files>
  <read_first>src/lib/auth.ts, src/app/api/matches/[id]/route.ts, src/components/match/match-card.tsx</read_first>
  <action>
Create src/app/(dashboard)/page.tsx as Server Component:

1. Import: auth, getMatchById, redirect, MatchCard, Button, Card
2. Export default async function DashboardPage():
   - Get session via auth.api.getSession({ headers: await headers() })
   - Redirect to /login if no session
   - Get user's matches (query matches where createdBy = session.user.id, order by date DESC)
   - Separate into: upcoming (date >= now, status in [draft, open, full]) and recent (date < now or status in [locked, played, rated])
3. Render layout:
   - Header with:
     * User name greeting
     * Primary button: "Nouveau match" (links to /matches/new)
     * Dropdown menu: profile, logout
   - Section 1: "Prochain match"
     * If upcoming exists: show MatchCard with variant="upcoming", add "Voir le match" button
     * If no upcoming: show empty state "Aucun match à venir — Crée ton premier match !"
   - Section 2: "Mes matchs"
     * Show last 5 recent matches as MatchCard with variant="recent"
     * "Voir tout" link (to /dashboard/matches - future page)
   - Mobile navigation: bottom tab bar with icons (Accueil, Matchs, Groupes, Profil) - Phase 3
4. Use mobile-first layout (max-w-2xl mx-auto, px-4)
5. Add loading skeleton and error boundary

Per REQUIREMENTS.md MATCH-07 and Phase 3 dashboard requirements.
  </action>
  <verify>
    <automated>grep -q "Prochain match" src/app/\(dashboard\)/page.tsx && grep -q "Mes matchs" src/app/\(dashboard\)/page.tsx && grep -q "Nouveau match" src/app/\(dashboard\)/page.tsx && grep -q "MatchCard" src/app/\(dashboard\)/page.tsx</automated>
  </verify>
  <done>
    - Dashboard page accessible at /dashboard (protected route)
    - Shows upcoming match with confirmed count
    - Shows 5 recent matches
    - "Nouveau match" CTA links to creation form
    - User greeting and logout functionality
    - Mobile-first layout with bottom nav placeholder
    - Loading and error states
  </done>
</task>

</tasks>

<verification>
1. Run pnpm typecheck to verify TypeScript compilation
2. Run pnpm build to ensure no build errors
3. Test waitlist promotion flow:
   - Create match with maxPlayers=2
   - RSVP 3 players (2 confirmed, 1 waitlisted at position 1)
   - First confirmed player cancels
   - Verify waitlisted player auto-promoted to confirmed
   - Verify waitlist position now 0
4. Test concurrent RSVP + promotion scenarios (critical for coordination):
   - Create match with maxPlayers=10, 9 confirmed, 1 waitlisted
   - Simultaneously: cancel 1 confirmed AND RSVP 1 new guest
   - Verify transaction handles both operations correctly
   - Verify final match.status is correct (should be "full" if 10 confirmed)
5. Test dashboard access:
   - Visit /dashboard (redirects to /login if not authenticated)
   - Verify upcoming match shown with correct confirmed count
   - Verify recent matches shown
   - Click "Nouveau match" → redirects to /matches/new
6. Test match card navigation:
   - Click upcoming match → redirects to /m/{shareToken}
   - Verify status badge color correct
</verification>

<success_criteria>
1. Waitlisted players automatically promoted when spot opens
2. Promotion maintains FIFO order (first waitlisted gets first spot)
3. Match status correctly updates during promotion
4. Concurrent RSVP + promotion operations handled correctly via FOR UPDATE locking
5. Dashboard accessible only when authenticated
6. Dashboard shows upcoming match with confirmed count
7. Dashboard shows recent matches
8. "Nouveau match" button links to creation form
9. Match cards display correct status badges
10. Mobile viewport displays correctly
11. All TypeScript types valid
12. Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-match-creation-guest-rsvp/02-03-SUMMARY.md` with:
- Waitlist promotion strategy (FIFO via confirmedAt ordering)
- Concurrency coordination with 02-02 RSVP operations (FOR UPDATE locking)
- Dashboard data fetching approach
- Mobile navigation considerations
- Email notification hook (for Phase 10 integration)
- Any deviations from CONTEXT.md decisions
</output>
