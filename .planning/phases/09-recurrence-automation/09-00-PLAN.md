---
phase: 09-recurrence-automation
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/unit/recurrence.test.ts
autonomous: true
requirements:
  - RECUR-01
  - RECUR-02
  - RECUR-04

must_haves:
  truths:
    - "Test file exists with stubs covering RECUR-01, RECUR-02, RECUR-04"
    - "Test file can run with pnpm test command"
    - "Tests cover date calculation edge cases (DST, leap year)"
    - "Tests cover CRON_SECRET validation logic"
    - "Tests cover duplicate occurrence prevention"
  artifacts:
    - path: "src/lib/__tests__/unit/recurrence.test.ts"
      provides: "Test stubs for recurrence functionality"
      min_lines: 50
      contains: "describe, it, expect"
  key_links: []
---

<objective>
Create test stub file for recurrence functionality. This Wave 0 plan establishes the test infrastructure that subsequent plans will implement against. The stubs define the expected behavior for date calculations, CRON_SECRET validation, and duplicate prevention, enabling TDD for Plan 01.

Purpose: Satisfy Nyquist compliance by creating test file before implementation. Enable red-green-refactor cycle for recurrence logic.
Output: Test stub file at src/lib/__tests__/unit/recurrence.test.ts with pending tests for all recurrence requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-recurrence-automation/09-CONTEXT.md
@.planning/phases/09-recurrence-automation/09-VALIDATION.md
@.planning/phases/09-recurrence-automation/09-01-PLAN.md

# Test Framework Context

From vitest.config.ts (existing):
- Vitest 4.1.2 configured
- Test environment: jsdom
- Test files match: **/__tests__/**/*.test.ts
- Coverage: c8 provider

From package.json:
- vitest: ^4.1.2 (installed)
- Test command: pnpm test (runs all tests)
- Target command: pnpm test src/lib/__tests__/unit/recurrence.test.ts

# Key Decisions from CONTEXT.md

**D-03:** Use date-fns addWeeks() to calculate next occurrence (+7 days)
**D-11:** Protect cron endpoint with CRON_SECRET header validation
**D-12:** Return 401 if secret doesn't match, log failed attempts

# Requirements to Cover

**RECUR-01:** Cron job runs daily at midnight UTC to check for recurring matches
**RECUR-02:** System finds parent matches (recurrence='weekly', parentMatchId IS NULL) that need next occurrence
**RECUR-04:** New occurrences have different shareToken, status='open', calculated date, no players auto-confirmed
</context>

<feature>
  <name>Recurrence Test Infrastructure</name>
  <files>src/lib/__tests__/unit/recurrence.test.ts</files>
  <behavior>
    RED Phase (Failing Tests):

    1. **Date Calculation Tests (RECUR-01):**
       - "addWeeks() adds 7 days to match date" → expect addWeeks(date, 1) to be date + 7 days
       - "addWeeks() handles DST transition correctly" → expect time of day to remain consistent across DST boundary
       - "addWeeks() handles leap year dates correctly" → expect Feb 29 + 1 week to be Mar 7 (or Mar 8 in non-leap year)

    2. **Query Logic Tests (RECUR-02):**
       - "getParentMatchesNeedingNextOccurrence filters for weekly recurrence" → expect results to have recurrence="weekly"
       - "getParentMatchesNeedingNextOccurrence filters for parent matches only" → expect results to have parentMatchId IS NULL
       - "getParentMatchesNeedingNextOccurrence excludes parents with existing child match" → expect parent with existing child to NOT be in results

    3. **Match Creation Tests (RECUR-04):**
       - "createRecurringMatchOccurrence generates new shareToken" → expect child.shareToken !== parent.shareToken
       - "createRecurringMatchOccurrence sets status to open" → expect child.status === "open"
       - "createRecurringMatchOccurrence sets recurrence to none" → expect child.recurrence === "none"
       - "createRecurringMatchOccurrence does NOT copy players" → expect no match_players records for child match

    4. **Security Tests (D-11, D-12):**
       - "cron endpoint returns 401 without CRON_SECRET" → expect response status 401 when auth header missing/invalid
       - "cron endpoint returns 500 when CRON_SECRET env var missing" → expect response status 500 when !process.env.CRON_SECRET
  </behavior>
  <implementation>
    GREEN Phase (Implementation):

    All tests in this file are stubs only. Implementation happens in Plan 01 tasks:
    - Task 3 implements getParentMatchesNeedingNextOccurrence (covers query logic tests)
    - Task 4 implements createRecurringMatchOccurrence (covers match creation tests)
    - Task 5 implements cron endpoint (covers security tests)

    Run pnpm test src/lib/__tests__/unit/recurrence.test.ts after each task to verify tests go from RED → GREEN.
  </implementation>
</feature>

<verification>
1. **Test File Exists:** src/lib/__tests__/unit/recurrence.test.ts created
2. **Test Runner Executes:** pnpm test src/lib/__tests__/unit/recurrence.test.ts runs without crashing
3. **All Tests Failing (RED):** expect() assertions fail because implementation doesn't exist yet
4. **Test Count:** At least 8 test cases covering RECUR-01, RECUR-02, RECUR-04, and security
5. **Nyquist Compliance:** Wave 0 requirements satisfied before Plan 01 execution
</verification>

<success_criteria>
1. Test file created at src/lib/__tests__/unit/recurrence.test.ts
2. pnpm test src/lib/__tests__/unit/recurrence.test.ts runs successfully (all tests fail as expected)
3. All test stubs map to requirements (RECUR-01, RECUR-02, RECUR-04)
4. Edge cases covered (DST, leap year, duplicate prevention)
5. Plan 01 can now execute with tests already in place
</success_criteria>

<output>
After completion, create `.planning/phases/09-recurrence-automation/09-00-SUMMARY.md` with:
- Test stub file created
- All tests failing (RED state)
- Ready for Plan 01 implementation (GREEN phase)
</output>
