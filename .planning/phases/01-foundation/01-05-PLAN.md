---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-04
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/components/auth/auth-tabs.tsx
  - src/components/auth/login-form.tsx
  - src/components/auth/register-form.tsx
  - src/components/auth/magic-link-form.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "User sees tabbed interface with Connexion and Inscription"
    - "User can fill email/password and log in"
    - "User can fill name/email/password and register"
    - "User can request magic link with email only"
    - "Form shows inline errors AND toast summary"
    - "Remember me checkbox is visible"
    - "After auth, user is redirected to /dashboard"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Auth page with tabs"
      min_lines: 10
    - path: "src/components/auth/auth-tabs.tsx"
      provides: "Tabbed container (Connexion/Inscription)"
      contains: "Tabs"
    - path: "src/components/auth/login-form.tsx"
      provides: "Login form with email, password, remember me"
      contains: "useForm"
    - path: "src/components/auth/register-form.tsx"
      provides: "Register form with name, email, password"
      contains: "signUp"
  key_links:
    - from: "src/components/auth/login-form.tsx"
      to: "src/lib/auth-client.ts"
      via: "signIn import"
      pattern: "import.*signIn.*from"
    - from: "src/components/auth/auth-tabs.tsx"
      to: "src/components/ui/tabs.tsx"
      via: "Tabs import"
      pattern: "import.*Tabs.*from"
---

<objective>
Create the authentication UI with tabbed login/register forms per user decisions.

Purpose: Enable users to create accounts and log in with a mobile-first, zero-friction interface.
Output: Working /login page with Connexion and Inscription tabs.
</objective>

<context>
@CLAUDE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<interfaces>
<!-- From Plan 04 -->
From src/lib/auth-client.ts:
```typescript
export const signIn: AuthClient["signIn"];
export const signUp: AuthClient["signUp"];
export const useSession: () => Session | null;
```

<!-- From Plan 02 -->
shadcn/ui components available:
- Button, Input, Label, Checkbox from @/components/ui/*
- Tabs, TabsList, TabsTrigger, TabsContent from @/components/ui/tabs
- Card, CardHeader, CardContent, CardFooter from @/components/ui/card
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create auth page with route group</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/layout.tsx</files>
  <action>
Create src/app/(auth)/layout.tsx for auth route group:
```tsx
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background px-4 py-8">
      <div className="w-full max-w-md">
        {children}
      </div>
    </div>
  );
}
```

Create src/app/(auth)/login/page.tsx (per D-01: single page with tabs):
```tsx
import { AuthTabs } from "@/components/auth/auth-tabs";

export const metadata = {
  title: "Connexion | kickoff",
  description: "Connecte-toi ou cree ton compte kickoff",
};

export default function LoginPage() {
  return (
    <div className="space-y-6">
      <div className="text-center">
        <h1 className="text-3xl font-bold text-primary">kickoff</h1>
        <p className="text-muted-foreground mt-2">
          Organise tes matchs de foot
        </p>
      </div>
      <AuthTabs />
    </div>
  );
}
```
  </action>
  <verify>
    <automated>test -f src/app/\(auth\)/login/page.tsx &amp;&amp; test -f src/app/\(auth\)/layout.tsx</automated>
  </verify>
  <done>Auth route group and login page created.</done>
</task>

<task type="auto">
  <name>Task 2: Create AuthTabs component with forms</name>
  <files>src/components/auth/auth-tabs.tsx, src/components/auth/login-form.tsx, src/components/auth/register-form.tsx</files>
  <action>
Install react-hook-form and zod for form validation:
```bash
pnpm add react-hook-form @hookform/resolvers zod
```

Create src/components/auth/auth-tabs.tsx (per D-01: tabs on one page):
```tsx
"use client";

import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { LoginForm } from "./login-form";
import { RegisterForm } from "./register-form";

export function AuthTabs() {
  return (
    <Tabs defaultValue="login" className="w-full">
      <TabsList className="grid w-full grid-cols-2">
        <TabsTrigger value="login">Connexion</TabsTrigger>
        <TabsTrigger value="register">Inscription</TabsTrigger>
      </TabsList>
      <TabsContent value="login">
        <Card>
          <CardHeader>
            <CardTitle>Connexion</CardTitle>
            <CardDescription>
              Entre tes identifiants pour te connecter
            </CardDescription>
          </CardHeader>
          <CardContent>
            <LoginForm />
          </CardContent>
        </Card>
      </TabsContent>
      <TabsContent value="register">
        <Card>
          <CardHeader>
            <CardTitle>Inscription</CardTitle>
            <CardDescription>
              Cree ton compte pour organiser tes matchs
            </CardDescription>
          </CardHeader>
          <CardContent>
            <RegisterForm />
          </CardContent>
        </Card>
      </TabsContent>
    </Tabs>
  );
}
```

Create src/components/auth/login-form.tsx (per D-02, D-03, D-04):
```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
import { toast } from "sonner";
import { signIn } from "@/lib/auth-client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Checkbox } from "@/components/ui/checkbox";
import { MagicLinkForm } from "./magic-link-form";

const loginSchema = z.object({
  email: z.string().email("Email invalide"),
  password: z.string().min(8, "Le mot de passe doit faire au moins 8 caracteres"),
  rememberMe: z.boolean().default(false),
});

type LoginFormData = z.infer<typeof loginSchema>;

export function LoginForm() {
  const router = useRouter();
  const [isLoading, setIsLoading] = useState(false);
  const [showMagicLink, setShowMagicLink] = useState(false);

  const {
    register,
    handleSubmit,
    formState: { errors },
    setValue,
    watch,
  } = useForm<LoginFormData>({
    resolver: zodResolver(loginSchema),
    defaultValues: {
      rememberMe: false,
    },
  });

  const rememberMe = watch("rememberMe");

  const onSubmit = async (data: LoginFormData) => {
    setIsLoading(true);
    try {
      const result = await signIn.email({
        email: data.email,
        password: data.password,
        rememberMe: data.rememberMe,
        callbackURL: "/dashboard",
      });

      if (result.error) {
        // Per D-03: inline errors AND toast summary
        toast.error("Connexion echouee", {
          description: result.error.message || "Email ou mot de passe incorrect",
        });
        return;
      }

      toast.success("Connexion reussie !");
      router.push("/dashboard");
      router.refresh();
    } catch (error) {
      toast.error("Une erreur est survenue");
    } finally {
      setIsLoading(false);
    }
  };

  if (showMagicLink) {
    return (
      <div className="space-y-4">
        <MagicLinkForm />
        <Button
          variant="link"
          className="w-full"
          onClick={() => setShowMagicLink(false)}
        >
          Retour a la connexion classique
        </Button>
      </div>
    );
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
      <div className="space-y-2">
        <Label htmlFor="email">Email</Label>
        <Input
          id="email"
          type="email"
          placeholder="ton@email.com"
          {...register("email")}
          aria-invalid={!!errors.email}
        />
        {errors.email && (
          <p className="text-sm text-destructive">{errors.email.message}</p>
        )}
      </div>

      <div className="space-y-2">
        <Label htmlFor="password">Mot de passe</Label>
        <Input
          id="password"
          type="password"
          placeholder="********"
          {...register("password")}
          aria-invalid={!!errors.password}
        />
        {errors.password && (
          <p className="text-sm text-destructive">{errors.password.message}</p>
        )}
      </div>

      {/* Per D-04: "Se souvenir de moi" checkbox */}
      <div className="flex items-center space-x-2">
        <Checkbox
          id="rememberMe"
          checked={rememberMe}
          onCheckedChange={(checked) => setValue("rememberMe", !!checked)}
        />
        <Label htmlFor="rememberMe" className="text-sm font-normal cursor-pointer">
          Se souvenir de moi
        </Label>
      </div>

      <Button type="submit" className="w-full" disabled={isLoading}>
        {isLoading ? "Connexion..." : "Se connecter"}
      </Button>

      {/* Per D-02: Magic link as secondary option */}
      <Button
        type="button"
        variant="ghost"
        className="w-full text-muted-foreground"
        onClick={() => setShowMagicLink(true)}
      >
        Connexion sans mot de passe
      </Button>
    </form>
  );
}
```

Create src/components/auth/register-form.tsx (per D-05: single "Nom" field):
```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
import { toast } from "sonner";
import { signUp } from "@/lib/auth-client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

// Per D-05: Single flexible "Nom" field, no format validation
const registerSchema = z.object({
  name: z.string().min(1, "Dis-nous comment tu t'appelles"),
  email: z.string().email("Email invalide"),
  password: z.string().min(8, "Le mot de passe doit faire au moins 8 caracteres"),
});

type RegisterFormData = z.infer<typeof registerSchema>;

export function RegisterForm() {
  const router = useRouter();
  const [isLoading, setIsLoading] = useState(false);

  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<RegisterFormData>({
    resolver: zodResolver(registerSchema),
  });

  const onSubmit = async (data: RegisterFormData) => {
    setIsLoading(true);
    try {
      const result = await signUp.email({
        name: data.name,
        email: data.email,
        password: data.password,
        callbackURL: "/dashboard",
      });

      if (result.error) {
        toast.error("Inscription echouee", {
          description: result.error.message || "Cet email est peut-etre deja utilise",
        });
        return;
      }

      toast.success("Bienvenue sur kickoff !");
      // Per D-06: Post-auth redirect to /dashboard
      router.push("/dashboard");
      router.refresh();
    } catch (error) {
      toast.error("Une erreur est survenue");
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
      {/* Per D-05: "Comment tu t'appelles ?" */}
      <div className="space-y-2">
        <Label htmlFor="name">Comment tu t'appelles ?</Label>
        <Input
          id="name"
          type="text"
          placeholder="Ton prenom, pseudo..."
          {...register("name")}
          aria-invalid={!!errors.name}
        />
        {errors.name && (
          <p className="text-sm text-destructive">{errors.name.message}</p>
        )}
      </div>

      <div className="space-y-2">
        <Label htmlFor="register-email">Email</Label>
        <Input
          id="register-email"
          type="email"
          placeholder="ton@email.com"
          {...register("email")}
          aria-invalid={!!errors.email}
        />
        {errors.email && (
          <p className="text-sm text-destructive">{errors.email.message}</p>
        )}
      </div>

      <div className="space-y-2">
        <Label htmlFor="register-password">Mot de passe</Label>
        <Input
          id="register-password"
          type="password"
          placeholder="8 caracteres minimum"
          {...register("password")}
          aria-invalid={!!errors.password}
        />
        {errors.password && (
          <p className="text-sm text-destructive">{errors.password.message}</p>
        )}
      </div>

      <Button type="submit" className="w-full" disabled={isLoading}>
        {isLoading ? "Inscription..." : "Creer mon compte"}
      </Button>
    </form>
  );
}
```
  </action>
  <verify>
    <automated>test -f src/components/auth/auth-tabs.tsx &amp;&amp; test -f src/components/auth/login-form.tsx &amp;&amp; test -f src/components/auth/register-form.tsx</automated>
  </verify>
  <done>Auth tabs with login and register forms created, following all D-01 to D-06 decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Create magic link form</name>
  <files>src/components/auth/magic-link-form.tsx</files>
  <action>
Create src/components/auth/magic-link-form.tsx (per D-02: secondary option):
```tsx
"use client";

import { useState } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
import { toast } from "sonner";
import { authClient } from "@/lib/auth-client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

const magicLinkSchema = z.object({
  email: z.string().email("Email invalide"),
});

type MagicLinkFormData = z.infer<typeof magicLinkSchema>;

export function MagicLinkForm() {
  const [isLoading, setIsLoading] = useState(false);
  const [emailSent, setEmailSent] = useState(false);

  const {
    register,
    handleSubmit,
    formState: { errors },
    getValues,
  } = useForm<MagicLinkFormData>({
    resolver: zodResolver(magicLinkSchema),
  });

  const onSubmit = async (data: MagicLinkFormData) => {
    setIsLoading(true);
    try {
      const result = await authClient.signIn.magicLink({
        email: data.email,
        callbackURL: "/dashboard",
      });

      if (result.error) {
        toast.error("Erreur", {
          description: result.error.message || "Impossible d'envoyer le lien",
        });
        return;
      }

      setEmailSent(true);
      toast.success("Lien envoye !", {
        description: "Verifie ta boite mail",
      });
    } catch (error) {
      toast.error("Une erreur est survenue");
    } finally {
      setIsLoading(false);
    }
  };

  if (emailSent) {
    return (
      <div className="text-center space-y-4">
        <div className="text-4xl">📧</div>
        <h3 className="font-semibold">Verifie ta boite mail</h3>
        <p className="text-sm text-muted-foreground">
          On a envoye un lien de connexion a{" "}
          <span className="font-medium">{getValues("email")}</span>
        </p>
        <p className="text-xs text-muted-foreground">
          Le lien expire dans 5 minutes
        </p>
      </div>
    );
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
      <div className="space-y-2">
        <Label htmlFor="magic-email">Email</Label>
        <Input
          id="magic-email"
          type="email"
          placeholder="ton@email.com"
          {...register("email")}
          aria-invalid={!!errors.email}
        />
        {errors.email && (
          <p className="text-sm text-destructive">{errors.email.message}</p>
        )}
      </div>

      <Button type="submit" className="w-full" disabled={isLoading}>
        {isLoading ? "Envoi..." : "Recevoir un lien de connexion"}
      </Button>

      <p className="text-xs text-center text-muted-foreground">
        On t'enverra un lien magique pour te connecter sans mot de passe
      </p>
    </form>
  );
}
```
  </action>
  <verify>
    <automated>test -f src/components/auth/magic-link-form.tsx &amp;&amp; grep -q "magicLink" src/components/auth/magic-link-form.tsx</automated>
  </verify>
  <done>Magic link form created with email confirmation state.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm build` completes successfully
- /login page shows tabbed interface
- Login form has email, password, remember me checkbox
- Register form has name, email, password fields
- Magic link option is accessible from login form
- Forms show inline errors on validation failure
- Toast notifications appear for success/error states
</verification>

<success_criteria>
- /login page with Connexion and Inscription tabs (D-01)
- Email/password as primary login method (D-02)
- Inline errors AND toast summary on validation (D-03)
- "Se souvenir de moi" checkbox present (D-04)
- Single "Nom" field with flexible input (D-05)
- Post-auth redirect to /dashboard (D-06)
- Magic link as secondary option
- Mobile-first responsive layout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
