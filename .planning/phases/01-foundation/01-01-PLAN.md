---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - .env.example
  - src/app/layout.tsx
  - src/app/globals.css
  - src/lib/utils.ts
autonomous: true
requirements:
  - PWA-01
  - PWA-04

must_haves:
  truths:
    - "Project runs with pnpm dev"
    - "TypeScript strict mode is enabled"
    - "Tailwind v4 CSS compiles correctly"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "src/app/layout.tsx"
      provides: "Root layout with metadata"
      min_lines: 15
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS with custom colors"
      contains: "@import"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "globals.css"
---

<objective>
Initialize the Next.js 15 project with TypeScript strict mode, Tailwind CSS v4, and project structure.

Purpose: Create the foundational project structure that all subsequent plans build upon.
Output: Working Next.js project that compiles and runs.
</objective>

<context>
@CLAUDE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 15 project with pnpm</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, .gitignore</files>
  <action>
Create Next.js 15 project using pnpm:
```bash
pnpm create next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack
```

After creation, verify and update tsconfig.json for strict mode:
```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    // ... rest of Next.js defaults
  }
}
```

Update next.config.ts to use TypeScript config format (Next.js 15 default).

Create .env.example with placeholder variables:
```
NEXT_PUBLIC_APP_URL=http://localhost:3000
DATABASE_URL=postgresql://...
BETTER_AUTH_SECRET=<random-32-chars>
BETTER_AUTH_URL=http://localhost:3000
RESEND_API_KEY=re_xxxxx
EMAIL_FROM=noreply@kickoff.app
```
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build</automated>
  </verify>
  <done>Next.js 15 project created, TypeScript strict mode enabled, builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v4 with kickoff colors</name>
  <files>src/app/globals.css</files>
  <action>
Per D-14 (app icon green background) and CLAUDE.md conventions, configure Tailwind v4 with CSS-first approach.

Update src/app/globals.css:
```css
@import "tailwindcss";

:root {
  /* kickoff brand colors */
  --color-primary: #2D5016;        /* vert terrain */
  --color-primary-foreground: #ffffff;
  --color-accent: #4ADE80;         /* accent green */
  --color-accent-foreground: #000000;

  /* shadcn/ui compatible variables */
  --background: 0 0% 100%;
  --foreground: 0 0% 3.9%;
  --card: 0 0% 100%;
  --card-foreground: 0 0% 3.9%;
  --popover: 0 0% 100%;
  --popover-foreground: 0 0% 3.9%;
  --primary: 101 60% 21%;          /* #2D5016 in HSL */
  --primary-foreground: 0 0% 100%;
  --secondary: 0 0% 96.1%;
  --secondary-foreground: 0 0% 9%;
  --muted: 0 0% 96.1%;
  --muted-foreground: 0 0% 45.1%;
  --accent: 142 69% 58%;           /* #4ADE80 in HSL */
  --accent-foreground: 0 0% 9%;
  --destructive: 0 84.2% 60.2%;
  --destructive-foreground: 0 0% 98%;
  --border: 0 0% 89.8%;
  --input: 0 0% 89.8%;
  --ring: 101 60% 21%;
  --radius: 0.5rem;
}

.dark {
  --background: 0 0% 3.9%;
  --foreground: 0 0% 98%;
  --card: 0 0% 3.9%;
  --card-foreground: 0 0% 98%;
  --popover: 0 0% 3.9%;
  --popover-foreground: 0 0% 98%;
  --primary: 101 60% 21%;
  --primary-foreground: 0 0% 100%;
  --secondary: 0 0% 14.9%;
  --secondary-foreground: 0 0% 98%;
  --muted: 0 0% 14.9%;
  --muted-foreground: 0 0% 63.9%;
  --accent: 142 69% 58%;
  --accent-foreground: 0 0% 9%;
  --destructive: 0 62.8% 30.6%;
  --destructive-foreground: 0 0% 98%;
  --border: 0 0% 14.9%;
  --input: 0 0% 14.9%;
  --ring: 101 60% 21%;
}

@theme inline {
  --color-background: hsl(var(--background));
  --color-foreground: hsl(var(--foreground));
  --color-primary: hsl(var(--primary));
  --color-primary-foreground: hsl(var(--primary-foreground));
  --color-accent: hsl(var(--accent));
  --color-accent-foreground: hsl(var(--accent-foreground));
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

Note: Tailwind v4 uses CSS-first configuration. Do NOT create tailwind.config.js.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Tailwind v4 configured with kickoff brand colors (#2D5016, #4ADE80), CSS compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Create root layout with metadata</name>
  <files>src/app/layout.tsx, src/lib/utils.ts</files>
  <action>
Update src/app/layout.tsx with kickoff metadata and mobile-first viewport:
```tsx
import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "kickoff",
  description: "Organise tes matchs de foot",
  applicationName: "kickoff",
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: "#2D5016",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="fr" suppressHydrationWarning>
      <body className={inter.className}>{children}</body>
    </html>
  );
}
```

Create src/lib/utils.ts (required for shadcn/ui):
```ts
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Install clsx and tailwind-merge:
```bash
pnpm add clsx tailwind-merge
```
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm dev --port 3000 &amp; sleep 5 &amp;&amp; curl -s http://localhost:3000 | grep -q "kickoff" &amp;&amp; pkill -f "next-server"</automated>
  </verify>
  <done>Root layout created with French lang, kickoff metadata, mobile viewport, utils.ts ready for shadcn/ui.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes with no errors
- `pnpm build` completes successfully
- `pnpm dev` starts development server
- Tailwind classes apply correctly
</verification>

<success_criteria>
- Next.js 15 project initialized with pnpm
- TypeScript strict mode enabled
- Tailwind v4 with kickoff colors (#2D5016, #4ADE80)
- Root layout with French lang and metadata
- Project builds without warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
