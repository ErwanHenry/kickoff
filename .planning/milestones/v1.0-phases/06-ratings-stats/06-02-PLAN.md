---
phase: 06-ratings-stats
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/m/[shareToken]/rate/page.tsx
  - src/app/match/[id]/rate/page.tsx
  - src/components/rating/rating-form.tsx
autonomous: true
requirements:
  - RATE-04
  - RATE-05
  - RATE-08
user_setup: []

must_haves:
  truths:
    - "Guest can access rating page via /m/{shareToken}/rate without account"
    - "User can access rating page via /match/{id}/rate when authenticated"
    - "Rating page displays all teammates (excluding self) who attended the match"
    - "Guest uses guest_token from cookie for identification"
    - "User uses session.user.id for identification"
    - "Both flows use the same RatingForm component"
    - "After submission, guest sees CTA to create account"
    - "After submission, user redirects to match detail"
    - "Match status updates to 'rated' when 50%+ players have rated"
  artifacts:
    - path: src/components/rating/rating-form.tsx
      provides: "Complete rating form with player cards"
      min_lines: 80
    - path: src/app/m/[shareToken]/rate/page.tsx
      provides: "Guest rating page (no auth required)"
      exports: ["default"]
    - path: src/app/match/[id]/rate/page.tsx
      provides: "User rating page (auth required)"
      exports: ["default"]
  key_links:
    - from: src/app/m/[shareToken]/rate/page.tsx
      to: src/components/rating/rating-form.tsx
      via: "component prop passing"
      pattern: "RatingForm"
    - from: src/app/match/[id]/rate/page.tsx
      to: src/components/rating/rating-form.tsx
      via: "component prop passing"
      pattern: "RatingForm"
    - from: src/app/m/[shareToken]/rate/page.tsx
      to: src/lib/actions/ratings.ts
      via: "Server Action import"
      pattern: "submitRatings"
    - from: src/app/match/[id]/rate/page.tsx
      to: src/lib/actions/ratings.ts
      via: "Server Action import"
      pattern: "submitRatings"
    - from: src/components/rating/rating-form.tsx
      to: src/lib/db/queries/ratings.ts
      via: "data fetching"
      pattern: "getMatchPlayersForRating|getExistingRatings"
---

<objective>
Create rating page routes for both guest and user flows, enabling players to rate teammates after a played match.

Purpose: Provide accessible rating interfaces for both authenticated users and guests, using a shared form component.
Output: Two working rating routes (/m/{shareToken}/rate and /match/{id}/rate) with proper auth handling and post-submission flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ratings-stats/06-CONTEXT.md
@.planning/phases/06-ratings-stats/06-01-PLAN.md

@src/app/m/[shareToken]/page.tsx (for guest flow patterns),
@src/app/match/[id]/attendance/page.tsx (for auth page patterns),
@src/lib/actions/ratings.ts (for submitRatings action),
@src/lib/db/queries/ratings.ts (for data fetching),
@src/components/rating/player-rating-card.tsx (from 06-01)
@src/components/rating/star-input.tsx (from 06-01)

# Design system
@src/lib/design-tokens.ts (colors, FootballIcon, statusBadges)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RatingForm component (shared between guest and user flows)</name>
  <files>src/components/rating/rating-form.tsx</files>
  <read_first>
    src/components/rating/player-rating-card.tsx (for card usage),
    src/components/match/attendance-form.tsx (for form patterns),
    src/lib/design-tokens.ts (for colors, spacing),
    src/lib/actions/ratings.ts (for submitRatings signature)
  </read_first>
  <action>
    Create src/components/rating/rating-form.tsx (client component):

    Props:
    - matchId: string (uuid)
    - shareToken?: string (for guest flow post-submit redirect)
    - players: Array of { id, name, avatar (initials) }
    - existingRatings: Array of { ratedId, technique, physique, collectif, comment }
    - isGuest: boolean (to determine post-submit flow)
    - submitRatings: (formData: FormData) => Promise<{ success?, error? }>

    State management:
    - ratings: Record<ratedId, { technique, physique, collectif, comment }>
    - Initialize from existingRatings prop (for editing/viewing)
    - Track touched fields (for validation)

    UI (mobile-first, max-w-2xl container):
    - Header:
      - Title: "Note tes coéquipiers" (font-sans, text-xl, font-semibold)
      - Subtitle: "Match du {date} à {location}" (text-sm, text-muted-foreground)
      - Progress indicator: "X/Y joueurs notés" (badge bg-lime-glow text-lime-dark)

    - Players list (space-y-4):
      - PlayerRatingCard for each player
      - Each card tracks ratings state
      - Scrollable with smooth scrolling

    - Sticky submit button (bottom, safe-area-inset):
      - Container: bg-chalk-pure, shadow-card-hover, p-4
      - Button:
        - Label: "Envoyer mes notes" (or "Terminé" if all rated)
        - Disabled: if no ratings provided
        - Loading state: spinner + "Envoi en cours..."
        - bg-pitch, text-chalk-pure, rounded-button
        - Full width, min-height 48px
      - Warning: show count of unrated players below button

    - Post-submit state:
      - Success message: "Merci pour tes notes !"
      - If guest: CTA card with "Crée un compte pour voir ton historique" → /register
      - If user: auto-redirect to /match/{matchId} after 2s

    Validation:
    - At least one rating required to submit
    - No validation on completeness (can rate subset of players)
    - Show warning if unrated players remain

    Toast notifications:
    - Success: "Notes envoyées avec succès !"
    - Error: "Erreur lors de l'envoi, réessaie"
  </action>
  <verify>
    <automated>grep -q "PlayerRatingCard" src/components/rating/rating-form.tsx && grep -q "Envoyer mes notes" src/components/rating/rating-form.tsx</automated>
  </verify>
  <done>
    RatingForm composes PlayerRatingCard for each player
    State management tracks ratings per player
    Submit button disabled when no ratings provided
    Shows progress indicator (X/Y players rated)
    Post-submit flow differs for guest (CTA) vs user (redirect)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create guest rating page route (/m/[shareToken]/rate)</name>
  <files>src/app/m/[shareToken]/rate/page.tsx</files>
  <read_first>
    src/app/m/[shareToken]/page.tsx (for guest cookie patterns),
    src/app/match/[id]/attendance/page.tsx (for page structure),
    src/lib/db/queries/ratings.ts (for data fetching)
  </read_first>
  <action>
    Create src/app/m/[shareToken]/rate/page.tsx (Server Component):

    Metadata:
    - title: "Noter le match — kickoff"
    - description: "Note tes coéquipiers sur 3 axes : technique, physique, collectif"

    Server-side logic:
    1. Parse shareToken from params
    2. Fetch match by shareToken (query needed: getMatchByShareToken)
       - If not found: notFound()
    3. Verify match status is "played"
       - If not played: Show warning "Ce match n'est pas encore terminé"
       - Link back to /m/{shareToken}
    4. Read guest_token from cookies:
       - const cookies = await cookies()
       - const guestToken = cookies.get("guest_token")?.value
       - If no guest_token: Show error "Non identifié" with link to /m/{shareToken}
    5. Verify guest participated (attended=true):
       - Query match_players where shareToken AND guest_token=guestToken AND attended=true
       - If not participated: Show "Tu n'étais pas présent à ce match"
    6. Fetch players to rate:
       - Call getMatchPlayersForRating(matchId, guestToken)
    7. Fetch existing ratings:
       - Call getExistingRatings(matchId, guestToken)
    8. Render RatingForm with:
       - matchId, shareToken
       - players (from step 6)
       - existingRatings (from step 7)
       - isGuest=true
       - submitRatings action

    Loading skeleton:
    - Similar to attendance page: card skeletons for players
    - Show while data fetching

    Error states:
    - Match not found: 404
    - Match not played: Warning card with back link
    - Guest not identified: Error card with "Rejoindre le match" link
    - Guest didn't attend: Error card with explanation

    Mobile-first:
    - container max-w-2xl mx-auto px-4 py-8 pb-24 (for sticky button)
    - Safe area insets for notched devices
  </action>
  <verify>
    <automated>grep -q "shareToken" src/app/m/[shareToken]/rate/page.tsx && grep -q "guest_token" src/app/m/[shareToken]/rate/page.tsx</automated>
  </verify>
  <done>
    Guest rating page accessible at /m/{shareToken}/rate without auth
    Reads guest_token from cookies for identification
    Verifies match status is "played" and guest attended
    Fetches players and existing ratings
    Renders RatingForm with isGuest=true
    Shows appropriate error states for various failure modes
  </done>
</task>

<task type="auto">
  <name>Task 3: Create user rating page route (/match/[id]/rate)</name>
  <files>src/app/match/[id]/rate/page.tsx</files>
  <read_first>
    src/app/match/[id]/attendance/page.tsx (for auth patterns),
    src/app/m/[shareToken]/rate/page.tsx (for reference structure),
    src/lib/auth.ts (for session pattern)
  </read_first>
  <action>
    Create src/app/match/[id]/rate/page.tsx (Server Component):

    Metadata:
    - title: "Noter le match — kickoff"
    - description: "Note tes coéquipiers sur 3 axes : technique, physique, collectif"

    Server-side logic:
    1. Parse matchId from params
    2. Auth check:
       - const session = await auth.api.getSession({ headers: await headers() })
       - If no session: redirect to /login?redirect=/match/{matchId}/rate
    3. Fetch match by matchId:
       - Call getMatchById(matchId)
       - If not found: notFound()
    4. Verify match status is "played":
       - If not played: Show warning "Ce match n'est pas encore terminé"
       - Link back to /match/{matchId}
    5. Verify user participated (attended=true):
       - Query match_players where matchId AND userId=session.user.id AND attended=true
       - If not participated: Show "Tu n'étais pas présent à ce match"
    6. Fetch players to rate:
       - Call getMatchPlayersForRating(matchId, session.user.id)
    7. Fetch existing ratings:
       - Call getExistingRatings(matchId, session.user.id)
    8. Render RatingForm with:
       - matchId
       - players (from step 6)
       - existingRatings (from step 7)
       - isGuest=false
       - submitRatings action

    Loading skeleton:
    - Same as guest page: card skeletons for players

    Error states:
    - Match not found: 404
    - Match not played: Warning card with back link
    - Not authenticated: redirect to /login
    - User didn't attend: Error card with explanation

    Mobile-first:
    - Same layout as guest page
    - container max-w-2xl mx-auto px-4 py-8 pb-24
  </action>
  <verify>
    <automated>grep -q "auth.api.getSession" src/app/match/[id]/rate/page.tsx && grep -q "getMatchPlayersForRating" src/app/match/[id]/rate/page.tsx</automated>
  </verify>
  <done>
    User rating page accessible at /match/{id}/rate with auth
    Uses session.user.id for identification
    Verifies match status is "played" and user attended
    Fetches players and existing ratings
    Renders RatingForm with isGuest=false
    Redirects unauthenticated users to login
  </done>
</task>

<task type="auto">
  <name>Task 4: Add getMatchByShareToken query and update match status check</name>
  <files>src/lib/db/queries/ratings.ts</files>
  <read_first>
    src/lib/db/queries/match-by-id.ts (for query patterns),
    src/db/schema.ts (for matches table structure)
  </read_first>
  <action>
    Update src/lib/db/queries/ratings.ts with:

    getMatchByShareToken(shareToken: string):
    - Query matches table where shareToken = shareToken
    - Return match object with:
      - id, title, date, location, status
      - Use Drizzle: db.select().from(matches).where(eq(matches.shareToken, shareToken)).limit(1)
    - Return null if not found

    verifyMatchPlayed(matchId: string):
    - Helper function to check match status
    - Query matches where id = matchId
    - Return true if status = "played", false otherwise
    - Use in both rating pages before rendering form
  </action>
  <verify>
    <automated>grep -q "getMatchByShareToken" src/lib/db/queries/ratings.ts && grep -q "verifyMatchPlayed" src/lib/db/queries/ratings.ts</automated>
  </verify>
  <done>
    getMatchByShareToken queries matches table by shareToken
    verifyMatchPlayed checks match status = "played"
    Both functions used in rating page routes
  </done>
</task>

</tasks>

<verification>
## Overall Verification Steps

1. **Guest Flow Check:**
   - Access /m/{shareToken}/rate without auth succeeds
   - Reads guest_token from cookies correctly
   - Shows error if guest didn't attend match
   - Shows error if match not played yet
   - Submitting ratings works for guest

2. **User Flow Check:**
   - Access /match/{id}/rate requires auth
   - Unauthenticated users redirected to /login
   - Uses session.user.id for identification
   - Shows error if user didn't attend match
   - Submitting ratings works for user

3. **Shared Form Check:**
   - RatingForm component used by both routes
   - Displays all attended players except rater
   - Tracks ratings state correctly
   - Submit button enables when ratings provided
   - Progress indicator updates (X/Y players rated)

4. **Post-Submit Flow Check:**
   - Guest sees CTA to create account after submission
   - User redirects to match detail after submission
   - Toast notifications appear for success/error

5. **Build Verification:**
   - pnpm typecheck passes
   - pnpm build succeeds
   - Both routes appear in build output
</verification>

<success_criteria>
1. Guest can rate at /m/{shareToken}/rate without account
2. User can rate at /match/{id}/rate with account
3. Both flows use same RatingForm component
4. Guest token read from cookies for identification
5. Session used for user identification
6. Only attended players can rate (verified via attended=true)
7. Only played matches can be rated (status="played")
8. Post-submit flow differs (guest CTA vs user redirect)
9. All pages mobile-optimized with proper spacing
</success_criteria>

<output>
After completion, create `.planning/phases/06-ratings-stats/06-02-SUMMARY.md` with:
- Files created/modified
- Route registration verification
- Guest flow test results
- User flow test results
- Next steps for plan 06-03 (stats recalculation refinement)
</output>
