---
phase: 07-player-profiles
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/m/[shareToken]/rate/page.tsx
  - src/app/match/[id]/rate/page.tsx
  - src/components/match/player-list.tsx
autonomous: true
requirements:
  - PROFILE-08
user_setup: []

must_haves:
  truths:
    - "Guest sees CTA to create account after rating"
    - "Player names link to their profile pages from match lists"
  artifacts:
    - path: src/app/m/[shareToken]/rate/page.tsx
      provides: "Guest rating page with CTA"
      contains: "Créer un compte"
    - path: src/app/match/[id]/rate/page.tsx
      provides: "User rating page with navigation"
      contains: "Voir mon profil"
    - path: src/components/match/player-list.tsx
      provides: "Clickable player names linking to profiles"
      contains: "href=/player/"
  key_links:
    - from: "src/components/match/player-list.tsx"
      to: "src/app/player/[id]/page.tsx"
      via: "player.id → href"
      pattern: "href=.*player/"
    - from: "src/app/m/[shareToken]/rate/page.tsx"
      to: "src/app/(auth)/register/page.tsx"
      via: "CTA button"
      pattern: "/register"
---

<objective>
Add navigation integrations for player profiles: guest CTA after rating, profile links from player lists, and user profile access. This completes the player profile feature by connecting it to existing workflows.

Purpose: Convert guest raters to registered users by showing CTA after rating. Enable profile discovery from match pages. Provide users access to their own profiles.
Output: Working profile links and CTAs in rating flow and match lists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-player-profiles/07-CONTEXT.md
@src/app/m/[shareToken]/rate/page.tsx
@src/app/match/[id]/rate/page.tsx
@src/components/match/player-list.tsx
@src/components/rating/rating-form.tsx
@.planning/phases/07-player-profiles/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add guest CTA after rating success</name>
  <files>src/app/m/[shareToken]/rate/page.tsx</files>
  <read_first>src/app/m/[shareToken]/rate/page.tsx, src/components/rating/rating-form.tsx</read_first>
  <action>Modify src/app/m/[shareToken]/rate/page.tsx:

After successful rating submission (already handled by RatingForm's onRatingSuccess callback), display a CTA section for guests.

1. Check if user is authenticated:
   - Use better-auth session check (if available in page props or header)
   - Alternative: Check if guest token cookie exists (already loaded in page)

2. If guest (not authenticated), add CTA section below the rating form:
   - Card container: bg-chalk-pure, shadow-card, rounded-card, p-6
   - Section spacing: mt-6 (margin top)
   - Icon: FootballIcon name="star" size={32} className="text-lime" (centered)
   - Heading: "Sauvegarde tes stats !" (text-center text-xl font-semibold mt-4 mb-2)
   - Description: "Crée un compte pour garder ton historique de matchs, voir tes stats, et accéder à ton profil complet." (text-center text-sm text-muted-foreground mb-6)
   - CTA Button:
     - Button variant: "default" (shadcn/ui)
     - bg-pitch, text-lime
     - Full width: w-full
     - Label: "Créer un compte gratuitement"
     - Link href: "/register" (use Button asChild with Link from next/link)
     - Icon: FootballIcon name="boot" size={16} (inline)

3. If authenticated user, show alternative CTA:
   - Card with same styling
   - Heading: "Merci pour tes notes !" (text-center text-xl font-semibold)
   - Description: "Retrouve toutes tes stats sur ton profil." (text-center text-sm text-muted-foreground mb-4)
   - Button: "Voir mon profil" → href="/player/[userId]" (use session.userId)

4. Implementation details:
   - This section should appear after rating submission (not before)
   - Pass onRatingSuccess callback to RatingForm that reveals the CTA section
   - Use useState to manage "rated" state (false → true after submission)
   - Fade-in animation: animate-in fade-in duration-300

5. Import requirements:
   - Button from @/components/ui/button
   - Link from next/link
   - FootballIcon from @/components/icons/football-icons
   - RatingForm from @/components/rating/rating-form

Page flow:
- Initial state: Show rating form only
- After submission: Show success message + CTA section (guest or user based on auth)
</action>
  <verify>
    <automated>grep -n "Créer un compte\|Voir mon profil\|Sauvegarde tes stats" src/app/m/[shareToken]/rate/page.tsx</automated>
  </verify>
  <done>Guest CTA displays after rating submission. Links to /register. User version links to their profile. Fade-in animation works.</done>
</task>

<task type="auto">
  <name>Task 2: Add profile CTA to user rating page</name>
  <files>src/app/match/[id]/rate/page.tsx</files>
  <read_first>src/app/match/[id]/rate/page.tsx, src/app/m/[shareToken]/rate/page.tsx</read_first>
  <action>Apply the same pattern from Task 1 to src/app/match/[id]/rate/page.tsx:

1. Copy the CTA section implementation from the guest rating page
2. Adapt for authenticated user context:
   - This page is only accessible to authenticated users (match/[id] requires auth)
   - Skip guest check, always show user CTA after rating
   - Profile link: Use session.userId (from better-auth)

3. CTA content for users:
   - Heading: "Merci pour tes notes !"
   - Description: "Retrouve toutes tes stats sur ton profil."
   - Button: "Voir mon profil" → href={`/player/${session.userId}`}
   - Same card styling and fade-in animation

4. Reuse the onRatingSuccess callback pattern:
   - Pass to RatingForm component
   - Reveal CTA section after successful submission
   - Use useState for "rated" state

5. Import requirements (same as Task 1)

Implementation note: This page is simpler than the guest version because we know the user is authenticated (protected route).
</action>
  <verify>
    <automated>grep -n "Voir mon profil\|Merci pour tes notes" src/app/match/[id]/rate/page.tsx</automated>
  </verify>
  <done>User CTA displays after rating submission. Links to /player/[userId]. Fade-in animation works.</done>
</task>

<task type="auto">
  <name>Task 3: Make player names clickable in player lists</name>
  <files>src/components/match/player-list.tsx</files>
  <read_first>src/components/match/player-list.tsx, src/lib/db/queries/matches.ts</read_first>
  <action>Modify src/components/match/player-list.tsx:

Current implementation displays player names as plain text. Update to make them clickable links to profile pages.

1. Update the player item rendering:
   - Wrap the player name in a Link component
   - Link destination:
     - If player.userId exists: href={`/player/${player.userId}`}
     - If player.userId is null (guest): Do NOT make clickable (guests don't have profiles yet)
   - Styling:
     - Remove default link underline: no-underline
     - Add hover state: hover:text-whistle-blue (blue)
     - Add transition: transition-colors
     - Keep existing font styling

2. For guests (no userId), display as plain text:
   - Add small tooltip or visual indicator that this is a guest
   - Option: Show "(invité)" suffix or use a different text color (text-muted-foreground)
   - Decision: Use text-muted-foreground for guest names to subtly indicate they're not registered users

3. Icon indicator (optional but recommended):
   - Add a small icon next to clickable names: FootballIcon name="star" size={12} (inline)
   - Only show for registered users (userId exists)
   - Color: text-lime (accent)

4. Import requirements:
   - Link from next/link
   - FootballIcon from @/components/icons/football-icons (if not already imported)

Example transformation:
Before:
<div>{player.name}</div>

After (for registered user):
<Link href={`/player/${player.userId}`} className="hover:text-whistle-blue transition-colors no-underline">
  {player.name}
</Link>

After (for guest):
<div className="text-muted-foreground">{player.name}</div>

Or with icon:
<Link href={`/player/${player.userId}`} className="hover:text-whistle-blue transition-colors no-underline flex items-center gap-1">
  <FootballIcon name="star" size={12} className="text-lime" />
  {player.name}
</Link>

Apply to all player list instances where names are displayed.
</action>
  <verify>
    <automated>grep -n "href.*player/\|Link.*player\|from 'next/link'" src/components/match/player-list.tsx</automated>
  </verify>
  <done>Player names with userId are clickable links to /player/[id]. Guest names are plain text with muted color. Hover state indicates clickability.</done>
</task>

</tasks>

<verification>
Build verification:
- pnpm typecheck passes (no TypeScript errors)
- pnpm build succeeds (no build warnings)

Code verification:
- Guest rating page shows CTA after submission
- User rating page shows profile link after submission
- Player list makes registered user names clickable
- Guest names in player list are not clickable
- All links point to correct /player/[id] routes

Functional verification:
- Submit rating as guest → see "Créer un compte" CTA
- Click CTA → navigate to /register
- Submit rating as user → see "Voir mon profil" CTA
- Click CTA → navigate to own profile
- View player list in match → registered user names are links
- Click player name → navigate to their profile page
- Guest names in list are not clickable
- Hover states work correctly
</verification>

<success_criteria>
1. Guest rating page displays account creation CTA after successful rating
2. User rating page displays profile link CTA after successful rating
3. Player names are clickable links in player lists (for registered users)
4. Guest names are not clickable (no userId)
5. All profile links navigate to correct pages
6. Visual feedback (hover states) indicates clickability
7. TypeScript type checking passes
8. Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-player-profiles/07-02-SUMMARY.md`
</output>
