---
phase: 10
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified: []
autonomous: false
requirements: [SHARE-01, SHARE-02, NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05, AUTH-05]
user_setup:
  - service: vercel
    why: "Production deployment for final validation"
    env_vars: []
    dashboard_config:
      - task: "Deploy to Vercel production"
        location: "Vercel Dashboard > kickoff > Deployments"
      - task: "Configure environment variables"
        location: "Vercel Dashboard > kickoff > Settings > Environment Variables"
      - task: "Verify cron jobs scheduled"
        location: "Vercel Dashboard > kickoff > Cron Jobs"
must_haves:
  truths:
    - "OG image preview displays correctly on WhatsApp (SHARE-01, SHARE-02)"
    - "All email notifications are delivered to inbox (NOTIF-01 through NOTIF-05)"
    - "Guest merge preserves all match history and ratings (AUTH-05)"
    - "App is installable as PWA on mobile devices"
    - "All pages are responsive on iPhone SE (375px) viewport"
    - "TypeScript compilation passes without errors"
    - "All tests pass (pnpm test)"
    - "Production deployment is live and functional"
  artifacts:
    - path: "VERIFICATION.md"
      provides: "Manual verification results for all Phase 10 features"
      min_lines: 100
  key_links:
    - from: "Manual verification"
      to: "WhatsApp link sharing"
      via: "Send match link to WhatsApp chat, verify preview"
      pattern: "m/.{token}.*preview"
    - from: "Manual verification"
      to: "Email delivery"
      via: "Check Resend dashboard, verify email in inbox"
      pattern: "resend.com/emails"
    - from: "Manual verification"
      to: "Guest merge flow"
      via: "Create guest, play match, rate, create account, verify profile"
      pattern: "merge.*history"
---

<objective>
Manual verification and production deployment of all Phase 10 features. This plan validates that OG images, email notifications, and guest merge work correctly in real-world conditions (WhatsApp, email clients, mobile devices).

Purpose: Quality gate before releasing to users — ensures all features work end-to-end
Output: VERIFICATION.md documenting all test results, live production deployment
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-polish-production/10-CONTEXT.md
@.planning/phases/10-polish-production/10-RESEARCH.md
@.planning/phases/10-polish-production/10-VALIDATION.md

# Manual Verification Requirements from VALIDATION.md
1. OG image preview on WhatsApp — SHARE-01, SHARE-02
2. Email delivery to inbox — NOTIF-01 through NOTIF-05
3. Guest merge preserves history — AUTH-05
4. PWA installable on mobile — PWA-01 through PWA-04
5. Responsive on iPhone SE (375px) — All requirements

# Pre-Deploy Checklist from RESEARCH.md D-19
- Test suite passes (pnpm test)
- Build verify (typecheck + lint)
- Device testing (iPhone SE, Android, tablets)
- Integration testing (OG images on WhatsApp, emails, guest→user merge)

# Locked Decisions from CONTEXT.md
D-19: Full pre-deploy validation: test suite, build verify, device testing, integration testing
D-20: PWA install verification — app must be installable on mobile browsers
D-21: Production deploy to Vercel with all environment variables configured
D-22: Claude's discretion on specific validation checklist order and execution

# Production Readiness Criteria
- All automated tests pass
- TypeScript compilation passes (pnpm typecheck)
- Linting passes (pnpm lint)
- Build succeeds (pnpm build)
- Environment variables configured in Vercel
- OG image generation works on Edge runtime
- Email sending configured with Resend API key
- Cron jobs scheduled in Vercel
- PWA manifest valid
- Service worker registered
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete automated test suite (Wave 0) + All Phase 10 features implemented (Plans 01-03)</what-built>
  <how-to-verify>
    1. Run test suite: `pnpm test --run`
    2. Verify all tests pass (no failures)
    3. Check test count: Expect ~33 tests from Wave 0 stubs
    4. Run TypeScript check: `pnpm typecheck`
    5. Verify no TypeScript errors
    6. Run linter: `pnpm lint`
    7. Verify no lint errors
    8. Run build: `pnpm build`
    9. Verify production build succeeds

    Expected results:
    - All tests pass (✓)
    - TypeScript compilation succeeds (✓)
    - Linting succeeds (✓)
    - Production build completes (✓)
  </how-to-verify>
  <resume-signal>Type "approved" when all automated checks pass, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dynamic OG image generation endpoint (/api/og?matchId={id})</what-built>
  <how-to-verify>
    1. Create a test match using seed data or manual creation
    2. Visit match page: https://your-domain.com/m/{shareToken}
    3. View page source, verify metadata:
       - og:title present with match title or "Match du [date]"
       - og:description with "8/14 confirmés • Lieu • Date"
       - og:image pointing to /api/og?matchId={uuid}
    4. Visit OG image URL directly in browser
    5. Verify image displays:
       - Background color #2D5016 (vert terrain)
       - Match title at top (52px, white)
       - Player count badge with lime #4ADE80 background
       - Location with 📍 icon
       - Date/time with 📅 icon, French locale (e.g., "8 avr. 20h00")
       - "kickoff" brand in bottom right
    6. Check image dimensions: Right-click > Inspect > 1200x630
    7. Test with long location: Create match with 30+ char location, verify truncation with ellipsis
    8. Test without title: Verify fallback "Match du [date]" displays

    Expected results:
    - OG image loads in <2s (Edge rendering)
    - All visual elements present and correct
    - French date formatting works
    - Truncation works for long text
  </how-to-verify>
  <resume-signal>Type "approved" when OG images render correctly, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>WhatsApp link sharing with OG preview integration</what-built>
  <how-to-verify>
    1. Create a test match (use seed script or manual creation)
    2. Copy match share link: https://your-domain.com/m/{shareToken}
    3. Send link to WhatsApp chat (or use WhatsApp link preview tool: https://developers.facebook.com/tools/debug/)
    4. Verify preview displays:
       - Match title or "Match du [date]"
       - Player count badge visible (8/14 confirmés)
       - Location and date/time visible
       - Kickoff branding visible
       - Image is 1200x630 (not stretched or cropped)
    5. Tap link to open app, verify it loads correctly

    Expected results:
    - WhatsApp preview shows OG image with all info
    - Preview matches actual image (not cached old version)
    - Link opens match page in browser
  </how-to-verify>
  <resume-signal>Type "approved" when WhatsApp preview works, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Email notification system with Resend integration (4 email types)</what-built>
  <how-to-verify>
    1. Configure test email recipient in environment
    2. Check Resend dashboard: https://resend.com/api-keys (verify API key valid)
    3. Test waitlist promotion email (NOTIF-01):
       - Create match with maxPlayers = 2
       - RSVP as guest 1, then guest 2 (match becomes full)
       - RSVP as guest 3 (goes to waitlist)
       - Cancel guest 1's RSVP (guest 3 promoted)
       - Check guest 3's email inbox for "Bonne nouvelle !" email
    4. Test welcome email (NOTIF-05):
       - Create new account with email
       - Check inbox for "Bienvenue sur kickoff !" email
    5. Test deadline reminder email (NOTIF-02):
       - Create match with deadline = 2 hours from now
       - RSVP as user with email
       - Manually trigger cron: curl https://your-domain.com/api/cron/deadline-reminders -H "Authorization: Bearer $CRON_SECRET"
       - Check inbox for "Plus que 2h pour confirmer" email
    6. Test post-match rating email (NOTIF-03):
       - Create match, RSVP as user
       - Close match (mark as played)
       - Check inbox for "Comment s'est passé le match ?" email

    For each email, verify:
    - Plain text format (no HTML)
    - French text with casual tone
    - Correct match information
    - Working link to match/rating page
    - Kickoff footer signature

    Expected results:
    - All 4 email types delivered
    - Emails arrive within 30 seconds
    - Not in spam folder
    - All information correct
  </how-to-verify>
  <resume-signal>Type "approved" when all emails work, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Guest-to-user account merge with data preservation</what-built>
  <how-to-verify>
    1. Create a guest RSVP:
       - Visit /m/{shareToken} (use incognito window)
       - Enter name "Test Guest", click "Je suis là !"
    2. Verify guest_token cookie set: DevTools > Application > Cookies > kickoff_guest_token
    3. Play the match (organizer marks match as played)
    4. Rate teammates as guest
    5. Click "Créer un compte" CTA (or visit /register)
    6. Register with email: test@example.com, password: Test1234
    7. After registration, verify:
       - Redirected to /dashboard
       - Profile shows match history (the match you played)
       - Profile shows ratings given and received
       - Player stats calculated correctly (matches played = 1)
       - guest_token cookie deleted
    8. Log out, log back in with new account
    9. Verify all data still present (not lost)

    Expected results:
    - Guest RSVP visible in profile
    - Ratings visible in profile
    - Player stats accurate
    - No data loss after merge
    - Cookie cleaned up
  </how-to-verify>
  <resume-signal>Type "approved" when guest merge works, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PWA manifest and service worker for mobile installability</what-built>
  <how-to-verify>
    Desktop Chrome:
    1. Open app in Chrome
    2. Open DevTools > Application > Manifest
    3. Verify manifest loads correctly:
       - name: "kickoff"
       - short_name: "kickoff"
       - start_url: "/dashboard"
       - display: "standalone"
       - background_color: "#ffffff"
       - theme_color: "#2D5016"
       - icons: 192x192 and 512x512 sizes
    4. Open Service Workers section
    5. Verify service worker registered and active
    6. Check Cache Storage: Verify app shell cached (layout, CSS, fonts)

    Mobile (iPhone or Android):
    1. Open app in mobile browser (Chrome or Safari)
    2. Verify "Add to Home Screen" prompt appears (or install via browser menu)
    3. Install app as PWA
    4. Launch from home screen
    5. Verify:
       - App opens in fullscreen (no browser UI)
       - All navigation works
       - Offline page loads (shows cached shell)
       - Back button works (in-app navigation)

    Expected results:
    - Manifest valid
    - Service worker active
    - Install prompt appears on mobile
    - App launches fullscreen from home screen
    - Offline mode shows cached shell
  </how-to-verify>
  <resume-signal>Type "approved" when PWA works, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive mobile-first UI for all pages</what-built>
  <how-to-verify>
    1. Open Chrome DevTools
    2. Toggle device toolbar (Cmd+Shift+M)
    3. Test on iPhone SE (375x667):
       - Dashboard page: verify all cards fit, no horizontal scroll
       - Match page: verify player list readable, buttons tappable (44x44px min)
       - Rating page: verify stars large enough for touch, textarea usable
       - Profile page: verify stats cards fit, radar chart readable
       - Groups page: verify leaderboard fits, member list scrollable
    4. Test on iPhone 14 (390x844):
       - Verify all pages use extra space effectively
       - No layout breaks
    5. Test on Android mid-range (412x915):
       - Verify all pages work
    6. Test on iPad (768x1024):
       - Verify app doesn't look stretched
       - Tablet layout works (if implemented)

    For each viewport, check:
    - No horizontal scroll
    - Text readable (no zoom needed)
    - Buttons tappable (44x44px minimum)
    - Images not stretched
    - Navigation works
    - Touch targets spaced properly

    Expected results:
    - All pages work on iPhone SE (smallest target)
    - No horizontal scroll
    - All text readable
    - All buttons tappable
  </how-to-verify>
  <resume-signal>Type "approved" when responsive UI works, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Production deployment to Vercel</what-built>
  <how-to-verify>
    1. Install Vercel CLI: `npm i -g vercel`
    2. Login: `vercel login`
    3. Deploy to production: `vercel --prod`
    4. Verify environment variables in Vercel Dashboard:
       - DATABASE_URL
       - RESEND_API_KEY
       - EMAIL_FROM
       - CRON_SECRET
       - NEXT_PUBLIC_APP_URL
    5. Verify cron jobs scheduled:
       - Go to Vercel Dashboard > kickoff > Cron Jobs
       - Verify /api/cron/recurring-matches scheduled (daily at midnight UTC)
       - Verify /api/cron/deadline-reminders scheduled (hourly)
    6. Test production URL:
       - Visit https://your-domain.com
       - Verify landing page loads
       - Create test match, verify OG image generation works
       - Send link to WhatsApp, verify preview
    7. Check Vercel logs:
       - Open Vercel Dashboard > kickoff > Logs
       - Verify no errors in Edge functions
       - Verify cron jobs executing

    Expected results:
    - App deployed to production URL
    - All environment variables configured
    - Cron jobs scheduled
    - All features work in production
  </how-to-verify>
  <resume-signal>Type "deployed" when production is live, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 8: Create verification summary document</name>
  <files>.planning/phases/10-polish-production/10-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/10-polish-production/10-VALIDATION.md (validation strategy)
    - All checkpoint results from previous tasks
  </read_first>
  <action>Create .planning/phases/10-polish-production/10-VERIFICATION.md documenting all manual verification results:

```markdown
# Phase 10 Verification Results

**Verified:** 2026-03-31
**Environment:** Production (Vercel)

## Automated Tests

✅ All tests pass (33/33)
- OG image generation: 9 tests
- Email templates: 12 tests
- Guest merge: 12 tests

✅ TypeScript compilation passes
✅ Linting passes
✅ Production build succeeds

## OG Image Generation (SHARE-01, SHARE-02)

✅ OG endpoint generates images correctly
- Dimensions: 1200x630
- Background: #2D5016
- All visual elements present
- French locale formatting
- Truncation works for long text
- Fallback title works

✅ WhatsApp link preview works
- Preview displays correctly
- All info visible
- Image loads fast

## Email Notifications (NOTIF-01 through NOTIF-05)

✅ Waitlist promotion email (NOTIF-01)
- Delivered within 30s
- Plain text format
- French casual tone
- Link works

✅ Deadline reminder email (NOTIF-02)
- Triggered by cron
- 2h timing correct
- Link works

✅ Post-match rating email (NOTIF-03)
- Sent after match closure
- Rating link works

✅ Welcome email (NOTIF-05)
- Sent after registration
- Onboarding message

## Guest Merge (AUTH-05)

✅ Guest creates account
- All RSVPs preserved
- All ratings preserved
- Stats calculated correctly
- Cookie deleted
- No data loss

✅ Edge cases handled
- No history: works
- Existing account: merges
- No cookie: normal registration

## PWA (PWA-01 through PWA-04)

✅ Manifest valid
✅ Service worker active
✅ Installable on mobile
✅ Fullscreen launch
✅ Offline mode works

## Responsive UI

✅ iPhone SE (375px)
- No horizontal scroll
- All text readable
- All buttons tappable

✅ iPhone 14 (390px)
✅ Android (412px)
✅ iPad (768px)

## Production Deployment

✅ Deployed to Vercel
✅ Environment variables configured
✅ Cron jobs scheduled
✅ All features work in production

## Issues Found

[Document any issues found during verification, or "None"]

## Sign-Off

Phase 10 is **PRODUCTION READY**.

Verified by: [Name]
Date: 2026-03-31
```

Fill in actual results from checkpoint tasks.</action>
  <verify>
    <automated>ls -la .planning/phases/10-polish-production/10-VERIFICATION.md && grep -q "Phase 10" .planning/phases/10-polish-production/10-VERIFICATION.md</automated>
  </verify>
  <done>Verification summary documents all test results and production readiness</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. VERIFICATION.md exists with all test results
2. Production deployment is live at Vercel URL
3. All checkpoint tasks approved
4. All features verified working in production
</verification>

<success_criteria>
- All automated tests pass (33/33)
- TypeScript compilation succeeds
- Production build succeeds
- OG images display correctly on WhatsApp
- All 4 email types delivered successfully
- Guest merge preserves all data
- PWA installable on mobile
- Responsive UI works on all viewports
- Production deployment live and functional
- VERIFICATION.md documents all results
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-production/10-04-SUMMARY.md`
</output>
