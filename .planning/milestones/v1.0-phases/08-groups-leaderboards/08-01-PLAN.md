---
phase: 08-groups-leaderboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/queries/groups.ts
  - src/lib/actions/groups.ts
  - src/lib/validations/group.ts
  - src/app/(dashboard)/groups/new/page.tsx
  - src/components/group/group-form.tsx
autonomous: false
requirements:
  - GROUP-01
  - GROUP-02
  - GROUP-08

must_haves:
  truths:
    - "Organisateur peut créer un groupe avec nom et slug auto-généré"
    - "Groupe génère un code d'invitation unique (6 chars)"
    - "Créateur du groupe devient captain automatiquement"
    - "Groupe peut avoir plusieurs organisateurs (captain/manager roles)"
  artifacts:
    - path: "src/lib/db/queries/groups.ts"
      provides: "Database queries for groups CRUD operations"
      exports: ["getGroupBySlug", "getUserGroups", "createGroup"]
    - path: "src/lib/actions/groups.ts"
      provides: "Server Actions for group creation"
      exports: ["createGroup"]
    - path: "src/app/(dashboard)/groups/new/page.tsx"
      provides: "Group creation page"
      min_lines: 30
    - path: "src/components/group/group-form.tsx"
      provides: "Group form component with name input"
      min_lines: 40
  key_links:
    - from: "src/components/group/group-form.tsx"
      to: "createGroup action"
      via: "use server action call"
      pattern: "createGroup\\({ name }\\)"
    - from: "createGroup action"
      to: "groups table"
      via: "db.insert(groups)"
      pattern: "db\\.insert\\(groups\\).*values"
    - from: "createGroup action"
      to: "group_members table"
      provides: "Add creator as captain"
      pattern: "db\\.insert\\(groupMembers\\).*role.*captain"
---

<objective>
Implement group creation functionality with auto-generated slug and invite code, allowing organizers to create groups and become captains automatically.

Purpose: Enable organizers to create groups for recurring matches, with unique identifiers for sharing and multi-organizer support.
Output: Working group creation form with slug generation, invite code generation, and captain role assignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-groups-leaderboards/08-CONTEXT.md

@src/db/schema.ts
@src/lib/design-tokens.ts
@src/lib/actions/teams.ts
@src/lib/db/queries/players.ts
</context>

<interfaces>
<!-- Key types and contracts from schema.ts -->

From src/db/schema.ts:
```typescript
export type Group = typeof groups.$inferSelect;
export type NewGroup = typeof groups.$inferInsert;
export type GroupMember = typeof groupMembers.$inferSelect;
export type NewGroupMember = typeof groupMembers.$inferInsert;

// groups table structure:
{
  id: uuid;
  name: string;
  slug: string; // unique
  createdBy: uuid; // references users.id
  inviteCode: string; // unique
  createdAt: Date;
  updatedAt: Date;
}

// group_members table structure:
{
  groupId: uuid; // PK component
  userId: uuid; // PK component
  role: 'captain' | 'manager' | 'player';
  joinedAt: Date;
}
```

From src/lib/design-tokens.ts:
```typescript
import { FootballIcon } from "@/components/icons/football-icons";

// Domain icon for groups:
FootballIcon name="cornerFlag"  // Use for group-related UI
```

From src/lib/utils.ts (standard utilities):
```typescript
import { cn } from "@/lib/utils"; // Class name merging
import { nanoid } from "@/lib/utils/ids"; // ID generation
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create group queries and validation schema</name>
  <files>src/lib/db/queries/groups.ts, src/lib/validations/group.ts</files>
  <read_first>
    - src/lib/db/queries/players.ts (for query pattern reference)
    - src/lib/validations/match.ts (for Zod validation pattern)
    - src/db/schema.ts (for groups and groupMembers table structure)
  </read_first>
  <action>
Create src/lib/db/queries/groups.ts with these functions:

1. **getGroupBySlug(slug: string)** - Fetch group by slug with creator and member count
   - Select from groups table
   - Join with users to get creator name
   - Aggregate member count from group_members
   - Returns: { id, name, slug, inviteCode, createdAt, createdBy, creatorName, memberCount }
   - Throw error if not found

2. **getUserGroups(userId: string)** - Get all groups where user is a member
   - Join groups → group_members → users
   - Select: id, name, slug, inviteCode, role, memberCount
   - Filter by userId in group_members
   - Order by createdAt DESC

3. **checkSlugExists(slug: string)** - Check if slug is already taken
   - Select count from groups where slug matches
   - Returns boolean

4. **checkInviteCodeExists(code: string)** - Check if invite code exists
   - Select count from groups where inviteCode matches
   - Returns boolean

Create src/lib/validations/group.ts:

1. **createGroupSchema** - Zod schema for group creation
   - name: string, min 3 chars, max 50 chars
   - slug: string, min 3 chars, max 50 chars (optional, auto-generated if not provided)
   - Use: z.object({ name: z.string().min(3).max(50), slug: z.string().min(3).max(50).optional() })

Use Drizzle query builders pattern from players.ts. Use sql operators for aggregation.
  </action>
  <verify>
    <automated>grep -n "export.*getGroupBySlug\|export.*getUserGroups\|export.*checkSlugExists\|export.*checkInviteCodeExists" src/lib/db/queries/groups.ts | wc -l | xargs -I {} test {} -ge 4</automated>
  </verify>
  <done>
Group queries file exists with 4 exported functions. Validation schema exists with createGroupSchema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create group Server Actions</name>
  <files>src/lib/actions/groups.ts</files>
  <read_first>
    - src/lib/actions/teams.ts (for Server Action pattern reference)
    - src/lib/db/queries/groups.ts (for queries to use)
    - src/lib/utils/ids.ts (for nanoid usage)
  </read_first>
  <action>
Create src/lib/actions/groups.ts with createGroup Server Action:

1. **createGroup(input: { name: string, slug?: string })** - Create a new group
   - Use 'use server' directive
   - Get session via auth.api.getSession({ headers: new Headers() })
   - Return { error: 'Non authentifié' } if no session
   - Parse input with createGroupSchema from src/lib/validations/group.ts
   - Extract name and optional slug from input
   - Auto-generate slug if not provided:
     - Lowercase the name
     - Replace spaces/accents with hyphens (use: name.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, ''))
     - Append random suffix if slug exists: slug-${nanoid(4)}
   - Generate unique invite code: nanoid(6) (ensure it's URL-safe)
   - Use db.transaction to:
     a. Insert into groups table: { id, name, slug, createdBy, inviteCode, createdAt, updatedAt }
     b. Insert into group_members table: { groupId, userId: session.user.id, role: 'captain', joinedAt }
   - Revalidate paths: revalidatePath('/dashboard/groups'), revalidatePath(`/group/${slug}`)
   - Return { success: true, data: { id, slug, inviteCode } }
   - Catch errors and return { error: 'Erreur lors de la création du groupe' }

Follow the exact pattern from teams.ts for session handling, transactions, and error responses.
  </action>
  <verify>
    <automated>grep -n "export async function createGroup\|use server" src/lib/actions/groups.ts | head -2</automated>
    <automated>grep -n "role: 'captain'\|nanoid(6)\|db.transaction" src/lib/actions/groups.ts | wc -l | xargs -I {} test {} -ge 3</automated>
  </verify>
  <done>
createGroup Server Action exists with captain role assignment, invite code generation, and transaction handling.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Group creation infrastructure with queries, validation, and Server Actions. The system can now create groups with auto-generated slugs, unique invite codes, and captain role assignment.</what-built>
  <how-to-verify>
1. Run `pnpm build` to verify TypeScript compilation
2. Run `pnpm typecheck` to verify no type errors
3. Check that createGroup action exists with:
   - `grep -n "createGroup" src/lib/actions/groups.ts`
4. Verify invite code generation:
   - `grep -n "nanoid(6)" src/lib/actions/groups.ts`
5. Verify captain role:
   - `grep -n "role.*captain" src/lib/actions/groups.ts`
6. Verify transaction handling:
   - `grep -n "db.transaction" src/lib/actions/groups.ts`

Expected output:
- No build errors
- No type errors
- createGroup function exists
- nanoid(6) for invite code
- Captain role in transaction
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to group creation UI, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create group form component</name>
  <files>src/components/group/group-form.tsx</files>
  <read_first>
    - src/components/match/match-form.tsx (for form pattern reference)
    - src/lib/design-tokens.ts (for colors and icons)
    - src/lib/validations/group.ts (for validation schema)
  </read_first>
  <action>
Create src/components/group/group-form.tsx as a client component ("use client"):

Props: { onSuccess?: (group: { slug: string, inviteCode: string }) => void }

UI Structure (mobile-first, max-w-md):
1. Card with bg-chalk-pure, shadow-card, rounded-card
2. Header: "Créer un groupe" (font-sans, text-xl, font-semibold)
3. Description: "Crée un groupe pour organiser des matchs récurrents avec tes potes" (text-sm, text-slate-mid)
4. Form fields:
   - **Name input** (from shadcn/ui):
     - Label: "Nom du groupe"
     - Placeholder: "Ex: Foot du mardi"
     - Required: true
     - Use: <Input id="name" name="name" required />
   - **Slug preview** (read-only, auto-generated):
     - Show "Slug: foot-du-mardi" below name input
     - Update in real-time as user types name
     - Use: <div className="text-sm text-slate-mid">Slug: {slugPreview}</div>
5. Submit button:
   - Text: "Créer le groupe"
   - Variant: default (bg-pitch, text-white)
   - Full width
   - Disabled while submitting
   - Use FootballIcon name="cornerFlag" as icon
6. Error message: Display action.error in red (text-red-card) if present
7. Loading state: Show "Création..." on button while submitting

State Management:
- name: string state for input
- isSubmitting: boolean state
- slugPreview: derived from name (useMemo)

Form Submission:
- Call createGroup({ name }) on form submit
- Handle success: call onSuccess(data) if provided, toast success
- Handle error: display error message
- Use toast from sonner for success/error feedback

Follow the mobile-first pattern from match-form.tsx with max-w-md container and vertical spacing.
  </action>
  <verify>
    <automated>grep -n "use client\|FootballIcon.*cornerFlag\|createGroup" src/components/group/group-form.tsx | wc -l | xargs -I {} test {} -ge 3</automated>
    <automated>grep -n "Nom du groupe\|Slug:" src/components/group/group-form.tsx | wc -l | xargs -I {} test {} -ge 2</automated>
  </verify>
  <done>
Group form component exists with name input, slug preview, submit button, and error handling.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create group creation page</name>
  <files>src/app/(dashboard)/groups/new/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/matches/new/page.tsx (for page pattern reference)
    - src/components/group/group-form.tsx (the form to use)
    - src/lib/auth.ts (for session handling)
  </read_first>
  <action>
Create src/app/(dashboard)/groups/new/page.tsx as a Server Component:

Page Layout:
1. Add metadata for SEO: title "Créer un groupe - kickoff", description "Crée un groupe pour organiser des matchs"
2. Check authentication:
   - Get session via auth.api.getSession({ headers: await headers() })
   - If not authenticated, redirect to /login with returnUrl
3. Main container:
   - max-w-2xl container with mx-auto, px-4 py-8
   - Breadcrumb/nav: "Dashboard → Groupes → Nouveau"
   - Page title: "Créer un groupe" (h1, font-sans, text-2xl, font-semibold)
   - Back button: Link to /dashboard/groups with chevron-left icon
4. Render GroupForm component with onSuccess callback:
   - On success: redirect to /group/{slug} via router.push()
   - Show success toast: "Groupe créé avec succès !"

Redirect Logic:
- After creation, redirect to the new group page
- Use: redirect(`/group/${slug}`) from next/navigation

Follow the page structure from matches/new/page.tsx with breadcrumbs and back navigation.
  </action>
  <verify>
    <automated>grep -n "metadata.*Créer un groupe\|import.*GroupForm\|redirect.*group" src/app/(dashboard)/groups/new/page.tsx | wc -l | xargs -I {} test {} -ge 3</automated>
  </verify>
  <done>
Group creation page exists with auth check, metadata, breadcrumb, and form integration.
  </done>
</task>

</tasks>

<verification>
1. Group queries file exists with all required functions
2. Validation schema exists for group creation
3. createGroup Server Action creates group with captain role
4. Invite code is unique 6-char string (nanoid)
5. Slug is auto-generated from name
6. Transaction ensures group + member atomicity
7. Group form component has name input and slug preview
8. Group creation page is protected by auth
9. Success redirects to group page
10. Mobile-first UI works on iPhone SE (375px)
</verification>

<success_criteria>
1. Organizer can create group with name
2. Slug auto-generates from name (lowercase, hyphens)
3. Invite code is unique 6-char string
4. Creator becomes captain automatically
5. Page redirects to group page after creation
6. Form shows slug preview in real-time
7. Success toast displays confirmation
8. No TypeScript errors
9. No build warnings
</success_criteria>

<output>
After completion, create `.planning/phases/08-groups-leaderboards/08-01-SUMMARY.md` with:
- Group creation flow complete
- Slug generation algorithm documented
- Invite code uniqueness verified
- Any deviations from plan
</output>
