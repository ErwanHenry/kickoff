# Plan: Retrofit Tokens & Badges

**Phase:** 01.1 - Setup Design System
**Created:** 2026-03-31
**Status:** complete
**Depends on:** 01.1-02b-PLAN.md (Retrofit Icons & Typography)

---

## Objective

Replace hardcoded badge configurations with `statusBadges` and `attendanceBadge()` tokens from the kickoff design system.

**Context:** Colors, icons, and typography are now updated. This phase finalizes the retrofit by using centralized badge tokens for consistent status and attendance displays.

---

## Wave 1: Replace Status Badges in Match Components

<task>
<id>01.1-02c-replace-status-badges</id>
<dependencies>01.1-02b-final-audit</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/match-card.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/player-list.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/rsvp-button.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/m/[shareToken]/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/match/[id]/teams/page.tsx</src>
</files>

<action>
Replace hardcoded badge configs with `statusBadges` from design-tokens.ts.

**1. Import statusBadges:**
```typescript
import { statusBadges } from "@/lib/design-tokens";
import { FootballIcon } from "@/components/icons/football-icons";
```

**2. MatchCard** (`match-card.tsx`):
```typescript
// Remove hardcoded statusConfig object:
const statusConfig: Record<string, ...> = { ... }; // DELETE THIS

// Replace with statusBadges:
const getStatusBadge = (status: string) => {
  const badge = statusBadges[status as keyof typeof statusBadges] || statusBadges.open;
  return badge;
};

const badge = getStatusBadge(match.status || "open");

// Update Badge component:
<Badge className={cn(badge.bg, badge.text, "flex-shrink-0 gap-1.5")}>
  <FootballIcon name={badge.icon} size={14} />
  {badge.label}
</Badge>
```

**3. PlayerList** (`player-list.tsx`):
```typescript
// Player status badges:
const playerBadge = statusBadges[player.status as keyof typeof statusBadges];

<Badge className={cn(playerBadge.bg, playerBadge.text, "gap-1.5")}>
  <FootballIcon name={playerBadge.icon} size={14} />
  {playerBadge.label}
</Badge>
```

**4. RSVPButton** (`rsvp-button.tsx`):
```typescript
// Match status badge:
const matchBadge = statusBadges[match.status as keyof typeof statusBadges];

<Badge className={cn(matchBadge.bg, matchBadge.text, "gap-1.5")}>
  <FootballIcon name={matchBadge.icon} size={14} />
  {matchBadge.label}
</Badge>
```

**5. Public Match Page** (`m/[shareToken]/page.tsx`):
```typescript
// Match header status badge:
const badge = statusBadges[match.status as keyof typeof statusBadges];

<Badge className={cn(badge.bg, badge.text, "gap-1.5")}>
  <FootballIcon name={badge.icon} size={16} />
  {badge.label}
</Badge>
```
</action>

<verify>
- `pnpm build` succeeds
- `pnpm typecheck` passes
- All status badges use `statusBadges` token
- Badges include FootballIcon from token
- Badge colors match design system (lime for confirmed, yellow for waitlist, etc.)
</verify>

<done>
All status badges use centralized `statusBadges` token with FootballIcon.
</done>
</task>

---

## Wave 2: Replace Attendance Badges

<task>
<id>01.1-02c-replace-attendance-badges</id>
<dependencies>01.1-02c-replace-status-badges</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/player-list.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/m/[shareToken]/page.tsx</src>
</files>

<action>
Replace hardcoded attendance rate badges with `attendanceBadge()` function.

**Note:** Attendance badges are typically shown on player profiles or player lists. If not yet implemented, this task prepares for when they are added.

**1. Import attendanceBadge:**
```typescript
import { attendanceBadge } from "@/lib/design-tokens";
```

**2. PlayerList (if attendance rate is displayed):**
```typescript
// Given a player's attendance rate:
const attendanceRate = player.attendanceRate || 0; // percentage
const badge = attendanceBadge(attendanceRate);

// Display badge:
<div className={cn("flex items-center gap-1.5 text-sm", badge.className)}>
  <span>{badge.emoji}</span>
  <span>{badge.label}</span>
</div>
```

**3. If attendance is not yet shown, add a placeholder comment:**
```typescript
// TODO: Display attendance badge when attendanceRate is available
// const badge = attendanceBadge(player.attendanceRate);
// <span className={badge.className}>{badge.emoji} {badge.label}</span>
```
</action>

<verify>
- `pnpm build` succeeds
- `pnpm typecheck` passes
- `attendanceBadge()` function is imported where needed
- Attendance displays use the badge function (or have TODO comment)
</verify>

<done>
Attendance badges use `attendanceBadge()` function for consistent display.
</done>
</task>

---

## Wave 3: Replace Shadow and Border Radius Tokens

<task>
<id>01.1-02c-replace-shadow-radius</id>
<dependencies>01.1-02c-replace-attendance-badges</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/match-card.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/player-list.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/team-reveal.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/draggable-player-card.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/attendance-form.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/install-prompt.tsx</src>
</files>

<action>
Replace hardcoded shadow and border-radius values with kickoff design system tokens.

**1. Card Components:**
Replace all shadow classes with kickoff tokens:
```typescript
// Replace:
className="shadow-sm hover:shadow-md"
// With:
className="shadow-card hover:shadow-card-hover"
```

**2. Border Radius:**
```typescript
// Cards:
className="rounded-card"  // 16px

// Badges:
className="rounded-badge"  // 20px

// Buttons:
className="rounded-button"  // 10px
```

**3. Specific Components:**

**MatchCard** (`match-card.tsx`):
```typescript
<Card className="shadow-card hover:shadow-card-hover rounded-card">
```

**PlayerList / DraggablePlayerCard** (`player-list.tsx`, `draggable-player-card.tsx`):
```typescript
<div className="shadow-card rounded-card bg-chalk-pure">
```

**TeamReveal** (`team-reveal.tsx`):
```typescript
<div className="shadow-card rounded-card border-pitch/20">
```

**AttendanceForm** (`attendance-form.tsx`):
```typescript
<Card className="shadow-card rounded-card">
```

**InstallPrompt** (`install-prompt.tsx`):
```typescript
<div className="shadow-card rounded-card">
```
</action>

<verify>
- `pnpm build` succeeds
- `pnpm typecheck` passes
- All cards use `shadow-card` and `rounded-card`
- All badges use `rounded-badge`
- Hover states use `shadow-card-hover`
- No hardcoded shadow values remain
- No hardcoded border-radius values remain
</verify>

<done>
All shadows and border-radius use kickoff design system tokens.
</done>
</task>

---

## Wave 4: Update Button Styles

<task>
<id>01.1-02c-update-buttons</id>
<dependencies>01.1-02c-replace-shadow-radius</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/rsvp-button.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/match/match-form.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/app/page.tsx</src>
<src>/Users/erwanhenry/claude-projects/kickoff/src/components/install-prompt.tsx</src>
</files>

<action>
Update button styles to use kickoff design system tokens.

**1. RSVPButton** (`rsvp-button.tsx`):
```typescript
// Primary state (not confirmed):
<Button className="bg-pitch hover:bg-pitch-light text-chalk-pure rounded-button shadow-btn-hover">

// Confirmed state:
<Button className="bg-lime-glow text-lime-dark rounded-button border border-lime">
```

**2. Landing Page CTA** (`page.tsx`):
```typescript
<Button className="bg-pitch hover:bg-pitch-light text-chalk-pure rounded-button shadow-btn-hover">
  Créer un match
</Button>
```

**3. Match Form** (`match-form.tsx`):
```typescript
// Submit button:
<Button className="bg-pitch hover:bg-pitch-light text-chalk-pure rounded-button shadow-btn-hover">

// Cancel button:
<Button variant="outline" className="rounded-button">
```

**4. Install Prompt** (`install-prompt.tsx`):
```typescript
<Button className="bg-pitch hover:bg-pitch-light text-chalk-pure rounded-button shadow-btn-hover">
```
</action>

<verify>
- `pnpm build` succeeds
- `pnpm typecheck` passes
- Primary buttons use `bg-pitch` with `shadow-btn-hover`
- Accent buttons use `bg-lime-glow`
- All buttons use `rounded-button`
- Hover states use pitch-light
</verify>

<done>
All buttons use kickoff design system colors and tokens.
</done>
</task>

---

## Wave 5: Final Token Verification

<task>
<id>01.1-02c-final-verification</id>
<dependencies>01.1-02c-update-buttons</dependencies>

<files>
<src>/Users/erwanhenry/claude-projects/kickoff/src</src>
</files>

<action>
Final verification that all design tokens are used consistently.

1. **Verify statusBadges usage:**
```bash
grep -r "statusBadges" src/ --include="*.tsx" -l
```
Expected: match-card, player-list, rsvp-button, public match page, teams page

2. **Verify attendanceBadge usage:**
```bash
grep -r "attendanceBadge" src/ --include="*.tsx" -l
```
Expected: player-list (if attendance is shown)

3. **Verify shadow tokens:**
```bash
grep -r "shadow-card" src/ --include="*.tsx" | grep -v "node_modules"
```
Expected: All Card components use shadow-card

4. **Verify border-radius tokens:**
```bash
grep -r "rounded-card\|rounded-badge\|rounded-button" src/ --include="*.tsx"
```
Expected: Consistent usage across components

5. **Visual verification:**
```bash
pnpm dev
```
Check:
- Status badges have correct colors (lime=confirmed, yellow=waitlist, red=no-show)
- Status badges include FootballIcon
- Cards have consistent elevation (shadow-card)
- Buttons have rounded corners (rounded-button)
- Badges are pill-shaped (rounded-badge)

6. **Check for any remaining hardcoded values:**
```bash
# Hardcoded shadows:
grep -r "shadow-\[0" src/ --include="*.tsx"

# Hardcoded border-radius:
grep -r "rounded-\[0" src/ --include="*.tsx"

# Hardcoded badge configs:
grep -r "variant=" src/ --include="*.tsx" | grep "Badge"
```
</action>

<verify>
- `statusBadges` used in all match components
- `attendanceBadge()` used where attendance is displayed
- `shadow-card` used on all cards
- `rounded-card`, `rounded-badge`, `rounded-button` used appropriately
- No hardcoded shadow values
- No hardcoded border-radius values
- No hardcoded badge variant configs
- `pnpm build` passes
- `pnpm typecheck` passes
- Visual verification complete
</verify>

<done>
Complete token retrofit — all badges, shadows, border-radius, and buttons use kickoff design system tokens.
</done>
</task>

---

## Success Criteria

- [ ] All status badges use `statusBadges` token
- [ ] All status badges include FootballIcon from token
- [ ] Attendance displays use `attendanceBadge()` function
- [ ] All cards use `shadow-card`
- [ ] Card hover states use `shadow-card-hover`
- [ ] Button hover states use `shadow-btn-hover`
- [ ] All cards use `rounded-card` (16px)
- [ ] All badges use `rounded-badge` (20px)
- [ ] All buttons use `rounded-button` (10px)
- [ ] No hardcoded shadow values
- [ ] No hardcoded border-radius values
- [ ] No hardcoded badge configs (variant prop)
- [ ] `pnpm build` passes
- [ ] `pnpm typecheck` passes

---

## Notes

- **Preserve functionality** — only replace tokens, not behavior
- **StatusBadge component** — consider creating a reusable `<StatusBadge status="confirmed" />` component in future
- **AttendanceBadge component** — consider creating `<AttendanceBadge rate={92} />` component in future
- **Design system complete** — after this phase, all UI should use kickoff tokens consistently
- **Next phases** — new features can now use the design system from the start
